// Code generated by sqlc. DO NOT EDIT.
// versions:
//   sqlc v1.31.1
// source: BackupImport.sql

package sqlc

import (
	"context"
)

const clearAccount = `-- name: ClearAccount :execrows
DELETE FROM account
`

func (q *Queries) ClearAccount(ctx context.Context) (int64, error) {
	result, err := q.db.Exec(ctx, clearAccount)
	if err != nil {
		return 0, err
	}
	return result.RowsAffected(), nil
}

const clearAccountGroup = `-- name: ClearAccountGroup :execrows
DELETE FROM account_group
`

func (q *Queries) ClearAccountGroup(ctx context.Context) (int64, error) {
	result, err := q.db.Exec(ctx, clearAccountGroup)
	if err != nil {
		return 0, err
	}
	return result.RowsAffected(), nil
}

const clearAccountGroupMember = `-- name: ClearAccountGroupMember :execrows
DELETE FROM account_group_member
`

func (q *Queries) ClearAccountGroupMember(ctx context.Context) (int64, error) {
	result, err := q.db.Exec(ctx, clearAccountGroupMember)
	if err != nil {
		return 0, err
	}
	return result.RowsAffected(), nil
}

const clearActivityEntry = `-- name: ClearActivityEntry :execrows
DELETE FROM activity_entry
`

func (q *Queries) ClearActivityEntry(ctx context.Context) (int64, error) {
	result, err := q.db.Exec(ctx, clearActivityEntry)
	if err != nil {
		return 0, err
	}
	return result.RowsAffected(), nil
}

const clearAutoAssignPolicy = `-- name: ClearAutoAssignPolicy :execrows
DELETE FROM auto_assign_policy
`

func (q *Queries) ClearAutoAssignPolicy(ctx context.Context) (int64, error) {
	result, err := q.db.Exec(ctx, clearAutoAssignPolicy)
	if err != nil {
		return 0, err
	}
	return result.RowsAffected(), nil
}

const clearAutomationRule = `-- name: ClearAutomationRule :execrows
DELETE FROM automation_rule
`

func (q *Queries) ClearAutomationRule(ctx context.Context) (int64, error) {
	result, err := q.db.Exec(ctx, clearAutomationRule)
	if err != nil {
		return 0, err
	}
	return result.RowsAffected(), nil
}

const clearBucket = `-- name: ClearBucket :execrows
DELETE FROM bucket
`

func (q *Queries) ClearBucket(ctx context.Context) (int64, error) {
	result, err := q.db.Exec(ctx, clearBucket)
	if err != nil {
		return 0, err
	}
	return result.RowsAffected(), nil
}

const clearCalendarFeed = `-- name: ClearCalendarFeed :execrows
DELETE FROM calendar_feed
`

func (q *Queries) ClearCalendarFeed(ctx context.Context) (int64, error) {
	result, err := q.db.Exec(ctx, clearCalendarFeed)
	if err != nil {
		return 0, err
	}
	return result.RowsAffected(), nil
}

const clearComment = `-- name: ClearComment :execrows
DELETE FROM comment
`

func (q *Queries) ClearComment(ctx context.Context) (int64, error) {
	result, err := q.db.Exec(ctx, clearComment)
	if err != nil {
		return 0, err
	}
	return result.RowsAffected(), nil
}

const clearConsentRecord = `-- name: ClearConsentRecord :execrows
DELETE FROM consent_record
`

func (q *Queries) ClearConsentRecord(ctx context.Context) (int64, error) {
	result, err := q.db.Exec(ctx, clearConsentRecord)
	if err != nil {
		return 0, err
	}
	return result.RowsAffected(), nil
}

const clearContainer = `-- name: ClearContainer :execrows
DELETE FROM container
`

func (q *Queries) ClearContainer(ctx context.Context) (int64, error) {
	result, err := q.db.Exec(ctx, clearContainer)
	if err != nil {
		return 0, err
	}
	return result.RowsAffected(), nil
}

const clearCustomFieldDefinition = `-- name: ClearCustomFieldDefinition :execrows
DELETE FROM custom_field_definition
`

func (q *Queries) ClearCustomFieldDefinition(ctx context.Context) (int64, error) {
	result, err := q.db.Exec(ctx, clearCustomFieldDefinition)
	if err != nil {
		return 0, err
	}
	return result.RowsAffected(), nil
}

const clearItemAttachment = `-- name: ClearItemAttachment :execrows
DELETE FROM item_attachment
`

func (q *Queries) ClearItemAttachment(ctx context.Context) (int64, error) {
	result, err := q.db.Exec(ctx, clearItemAttachment)
	if err != nil {
		return 0, err
	}
	return result.RowsAffected(), nil
}

const clearItemLabel = `-- name: ClearItemLabel :execrows
DELETE FROM item_label
`

func (q *Queries) ClearItemLabel(ctx context.Context) (int64, error) {
	result, err := q.db.Exec(ctx, clearItemLabel)
	if err != nil {
		return 0, err
	}
	return result.RowsAffected(), nil
}

const clearItemMember = `-- name: ClearItemMember :execrows
DELETE FROM item_member
`

func (q *Queries) ClearItemMember(ctx context.Context) (int64, error) {
	result, err := q.db.Exec(ctx, clearItemMember)
	if err != nil {
		return 0, err
	}
	return result.RowsAffected(), nil
}

const clearJumbleEntry = `-- name: ClearJumbleEntry :execrows
DELETE FROM jumble_entry
`

func (q *Queries) ClearJumbleEntry(ctx context.Context) (int64, error) {
	result, err := q.db.Exec(ctx, clearJumbleEntry)
	if err != nil {
		return 0, err
	}
	return result.RowsAffected(), nil
}

const clearLabel = `-- name: ClearLabel :execrows
DELETE FROM label
`

func (q *Queries) ClearLabel(ctx context.Context) (int64, error) {
	result, err := q.db.Exec(ctx, clearLabel)
	if err != nil {
		return 0, err
	}
	return result.RowsAffected(), nil
}

const clearLegalHold = `-- name: ClearLegalHold :execrows
DELETE FROM legal_hold
`

func (q *Queries) ClearLegalHold(ctx context.Context) (int64, error) {
	result, err := q.db.Exec(ctx, clearLegalHold)
	if err != nil {
		return 0, err
	}
	return result.RowsAffected(), nil
}

const clearMediaObject = `-- name: ClearMediaObject :execrows
DELETE FROM media_object
`

func (q *Queries) ClearMediaObject(ctx context.Context) (int64, error) {
	result, err := q.db.Exec(ctx, clearMediaObject)
	if err != nil {
		return 0, err
	}
	return result.RowsAffected(), nil
}

const clearMembership = `-- name: ClearMembership :execrows
DELETE FROM membership
`

func (q *Queries) ClearMembership(ctx context.Context) (int64, error) {
	result, err := q.db.Exec(ctx, clearMembership)
	if err != nil {
		return 0, err
	}
	return result.RowsAffected(), nil
}

const clearNotificationPreference = `-- name: ClearNotificationPreference :execrows
DELETE FROM notification_preference
`

func (q *Queries) ClearNotificationPreference(ctx context.Context) (int64, error) {
	result, err := q.db.Exec(ctx, clearNotificationPreference)
	if err != nil {
		return 0, err
	}
	return result.RowsAffected(), nil
}

const clearRecurrenceRule = `-- name: ClearRecurrenceRule :execrows
DELETE FROM recurrence_rule
`

func (q *Queries) ClearRecurrenceRule(ctx context.Context) (int64, error) {
	result, err := q.db.Exec(ctx, clearRecurrenceRule)
	if err != nil {
		return 0, err
	}
	return result.RowsAffected(), nil
}

const clearReminder = `-- name: ClearReminder :execrows
DELETE FROM reminder
`

func (q *Queries) ClearReminder(ctx context.Context) (int64, error) {
	result, err := q.db.Exec(ctx, clearReminder)
	if err != nil {
		return 0, err
	}
	return result.RowsAffected(), nil
}

const clearRetentionPolicy = `-- name: ClearRetentionPolicy :execrows
DELETE FROM retention_policy
`

func (q *Queries) ClearRetentionPolicy(ctx context.Context) (int64, error) {
	result, err := q.db.Exec(ctx, clearRetentionPolicy)
	if err != nil {
		return 0, err
	}
	return result.RowsAffected(), nil
}

const clearSavedView = `-- name: ClearSavedView :execrows
DELETE FROM saved_view
`

func (q *Queries) ClearSavedView(ctx context.Context) (int64, error) {
	result, err := q.db.Exec(ctx, clearSavedView)
	if err != nil {
		return 0, err
	}
	return result.RowsAffected(), nil
}

const clearSetElement = `-- name: ClearSetElement :execrows
DELETE FROM set_element
`

func (q *Queries) ClearSetElement(ctx context.Context) (int64, error) {
	result, err := q.db.Exec(ctx, clearSetElement)
	if err != nil {
		return 0, err
	}
	return result.RowsAffected(), nil
}

const clearTemplate = `-- name: ClearTemplate :execrows
DELETE FROM template
`

func (q *Queries) ClearTemplate(ctx context.Context) (int64, error) {
	result, err := q.db.Exec(ctx, clearTemplate)
	if err != nil {
		return 0, err
	}
	return result.RowsAffected(), nil
}

const clearWebhookSubscription = `-- name: ClearWebhookSubscription :execrows
DELETE FROM webhook_subscription
`

func (q *Queries) ClearWebhookSubscription(ctx context.Context) (int64, error) {
	result, err := q.db.Exec(ctx, clearWebhookSubscription)
	if err != nil {
		return 0, err
	}
	return result.RowsAffected(), nil
}

const clearWorkItem = `-- name: ClearWorkItem :execrows
DELETE FROM work_item
`

func (q *Queries) ClearWorkItem(ctx context.Context) (int64, error) {
	result, err := q.db.Exec(ctx, clearWorkItem)
	if err != nil {
		return 0, err
	}
	return result.RowsAffected(), nil
}

const holdsAccount = `-- name: HoldsAccount :one
SELECT EXISTS (SELECT 1 FROM account WHERE id = ($1::jsonb->>'id')::uuid) AS held
`

func (q *Queries) HoldsAccount(ctx context.Context, payload []byte) (bool, error) {
	row := q.db.QueryRow(ctx, holdsAccount, payload)
	var held bool
	err := row.Scan(&held)
	return held, err
}

const holdsAccountGroup = `-- name: HoldsAccountGroup :one
SELECT EXISTS (SELECT 1 FROM account_group WHERE id = ($1::jsonb->>'id')::uuid) AS held
`

func (q *Queries) HoldsAccountGroup(ctx context.Context, payload []byte) (bool, error) {
	row := q.db.QueryRow(ctx, holdsAccountGroup, payload)
	var held bool
	err := row.Scan(&held)
	return held, err
}

const holdsAccountGroupMember = `-- name: HoldsAccountGroupMember :one
SELECT EXISTS (SELECT 1 FROM account_group_member WHERE group_id = ($1::jsonb->>'group_id')::uuid AND account_id = ($1::jsonb->>'account_id')::uuid) AS held
`

func (q *Queries) HoldsAccountGroupMember(ctx context.Context, payload []byte) (bool, error) {
	row := q.db.QueryRow(ctx, holdsAccountGroupMember, payload)
	var held bool
	err := row.Scan(&held)
	return held, err
}

const holdsActivityEntry = `-- name: HoldsActivityEntry :one
SELECT EXISTS (SELECT 1 FROM activity_entry WHERE id = ($1::jsonb->>'id')::uuid) AS held
`

func (q *Queries) HoldsActivityEntry(ctx context.Context, payload []byte) (bool, error) {
	row := q.db.QueryRow(ctx, holdsActivityEntry, payload)
	var held bool
	err := row.Scan(&held)
	return held, err
}

const holdsAutoAssignPolicy = `-- name: HoldsAutoAssignPolicy :one
SELECT EXISTS (SELECT 1 FROM auto_assign_policy WHERE id = ($1::jsonb->>'id')::uuid) AS held
`

func (q *Queries) HoldsAutoAssignPolicy(ctx context.Context, payload []byte) (bool, error) {
	row := q.db.QueryRow(ctx, holdsAutoAssignPolicy, payload)
	var held bool
	err := row.Scan(&held)
	return held, err
}

const holdsAutomationRule = `-- name: HoldsAutomationRule :one
SELECT EXISTS (SELECT 1 FROM automation_rule WHERE id = ($1::jsonb->>'id')::uuid) AS held
`

func (q *Queries) HoldsAutomationRule(ctx context.Context, payload []byte) (bool, error) {
	row := q.db.QueryRow(ctx, holdsAutomationRule, payload)
	var held bool
	err := row.Scan(&held)
	return held, err
}

const holdsBucket = `-- name: HoldsBucket :one
SELECT EXISTS (SELECT 1 FROM bucket WHERE id = ($1::jsonb->>'id')::uuid) AS held
`

func (q *Queries) HoldsBucket(ctx context.Context, payload []byte) (bool, error) {
	row := q.db.QueryRow(ctx, holdsBucket, payload)
	var held bool
	err := row.Scan(&held)
	return held, err
}

const holdsCalendarFeed = `-- name: HoldsCalendarFeed :one
SELECT EXISTS (SELECT 1 FROM calendar_feed WHERE id = ($1::jsonb->>'id')::uuid) AS held
`

func (q *Queries) HoldsCalendarFeed(ctx context.Context, payload []byte) (bool, error) {
	row := q.db.QueryRow(ctx, holdsCalendarFeed, payload)
	var held bool
	err := row.Scan(&held)
	return held, err
}

const holdsComment = `-- name: HoldsComment :one
SELECT EXISTS (SELECT 1 FROM comment WHERE id = ($1::jsonb->>'id')::uuid) AS held
`

func (q *Queries) HoldsComment(ctx context.Context, payload []byte) (bool, error) {
	row := q.db.QueryRow(ctx, holdsComment, payload)
	var held bool
	err := row.Scan(&held)
	return held, err
}

const holdsConsentRecord = `-- name: HoldsConsentRecord :one
SELECT EXISTS (SELECT 1 FROM consent_record WHERE id = ($1::jsonb->>'id')::uuid) AS held
`

func (q *Queries) HoldsConsentRecord(ctx context.Context, payload []byte) (bool, error) {
	row := q.db.QueryRow(ctx, holdsConsentRecord, payload)
	var held bool
	err := row.Scan(&held)
	return held, err
}

const holdsContainer = `-- name: HoldsContainer :one
SELECT EXISTS (SELECT 1 FROM container WHERE id = ($1::jsonb->>'id')::uuid) AS held
`

func (q *Queries) HoldsContainer(ctx context.Context, payload []byte) (bool, error) {
	row := q.db.QueryRow(ctx, holdsContainer, payload)
	var held bool
	err := row.Scan(&held)
	return held, err
}

const holdsCustomFieldDefinition = `-- name: HoldsCustomFieldDefinition :one
SELECT EXISTS (SELECT 1 FROM custom_field_definition WHERE id = ($1::jsonb->>'id')::uuid) AS held
`

func (q *Queries) HoldsCustomFieldDefinition(ctx context.Context, payload []byte) (bool, error) {
	row := q.db.QueryRow(ctx, holdsCustomFieldDefinition, payload)
	var held bool
	err := row.Scan(&held)
	return held, err
}

const holdsItemAttachment = `-- name: HoldsItemAttachment :one
SELECT EXISTS (SELECT 1 FROM item_attachment WHERE item_id = ($1::jsonb->>'item_id')::uuid AND media_id = ($1::jsonb->>'media_id')::uuid) AS held
`

func (q *Queries) HoldsItemAttachment(ctx context.Context, payload []byte) (bool, error) {
	row := q.db.QueryRow(ctx, holdsItemAttachment, payload)
	var held bool
	err := row.Scan(&held)
	return held, err
}

const holdsItemLabel = `-- name: HoldsItemLabel :one
SELECT EXISTS (SELECT 1 FROM item_label WHERE item_id = ($1::jsonb->>'item_id')::uuid AND label_id = ($1::jsonb->>'label_id')::uuid) AS held
`

func (q *Queries) HoldsItemLabel(ctx context.Context, payload []byte) (bool, error) {
	row := q.db.QueryRow(ctx, holdsItemLabel, payload)
	var held bool
	err := row.Scan(&held)
	return held, err
}

const holdsItemMember = `-- name: HoldsItemMember :one
SELECT EXISTS (SELECT 1 FROM item_member WHERE item_id = ($1::jsonb->>'item_id')::uuid AND account_id = ($1::jsonb->>'account_id')::uuid) AS held
`

func (q *Queries) HoldsItemMember(ctx context.Context, payload []byte) (bool, error) {
	row := q.db.QueryRow(ctx, holdsItemMember, payload)
	var held bool
	err := row.Scan(&held)
	return held, err
}

const holdsJumbleEntry = `-- name: HoldsJumbleEntry :one
SELECT EXISTS (SELECT 1 FROM jumble_entry WHERE id = ($1::jsonb->>'id')::uuid) AS held
`

func (q *Queries) HoldsJumbleEntry(ctx context.Context, payload []byte) (bool, error) {
	row := q.db.QueryRow(ctx, holdsJumbleEntry, payload)
	var held bool
	err := row.Scan(&held)
	return held, err
}

const holdsLabel = `-- name: HoldsLabel :one
SELECT EXISTS (SELECT 1 FROM label WHERE id = ($1::jsonb->>'id')::uuid) AS held
`

func (q *Queries) HoldsLabel(ctx context.Context, payload []byte) (bool, error) {
	row := q.db.QueryRow(ctx, holdsLabel, payload)
	var held bool
	err := row.Scan(&held)
	return held, err
}

const holdsLegalHold = `-- name: HoldsLegalHold :one
SELECT EXISTS (SELECT 1 FROM legal_hold WHERE id = ($1::jsonb->>'id')::uuid) AS held
`

func (q *Queries) HoldsLegalHold(ctx context.Context, payload []byte) (bool, error) {
	row := q.db.QueryRow(ctx, holdsLegalHold, payload)
	var held bool
	err := row.Scan(&held)
	return held, err
}

const holdsMediaObject = `-- name: HoldsMediaObject :one
SELECT EXISTS (SELECT 1 FROM media_object WHERE id = ($1::jsonb->>'id')::uuid) AS held
`

func (q *Queries) HoldsMediaObject(ctx context.Context, payload []byte) (bool, error) {
	row := q.db.QueryRow(ctx, holdsMediaObject, payload)
	var held bool
	err := row.Scan(&held)
	return held, err
}

const holdsMembership = `-- name: HoldsMembership :one
SELECT EXISTS (SELECT 1 FROM membership WHERE id = ($1::jsonb->>'id')::uuid) AS held
`

func (q *Queries) HoldsMembership(ctx context.Context, payload []byte) (bool, error) {
	row := q.db.QueryRow(ctx, holdsMembership, payload)
	var held bool
	err := row.Scan(&held)
	return held, err
}

const holdsNotificationPreference = `-- name: HoldsNotificationPreference :one
SELECT EXISTS (SELECT 1 FROM notification_preference WHERE account_id = ($1::jsonb->>'account_id')::uuid AND category = ($1::jsonb->>'category')::text AND channel = ($1::jsonb->>'channel')::text) AS held
`

func (q *Queries) HoldsNotificationPreference(ctx context.Context, payload []byte) (bool, error) {
	row := q.db.QueryRow(ctx, holdsNotificationPreference, payload)
	var held bool
	err := row.Scan(&held)
	return held, err
}

const holdsRecurrenceRule = `-- name: HoldsRecurrenceRule :one
SELECT EXISTS (SELECT 1 FROM recurrence_rule WHERE id = ($1::jsonb->>'id')::uuid) AS held
`

func (q *Queries) HoldsRecurrenceRule(ctx context.Context, payload []byte) (bool, error) {
	row := q.db.QueryRow(ctx, holdsRecurrenceRule, payload)
	var held bool
	err := row.Scan(&held)
	return held, err
}

const holdsReminder = `-- name: HoldsReminder :one
SELECT EXISTS (SELECT 1 FROM reminder WHERE id = ($1::jsonb->>'id')::uuid) AS held
`

func (q *Queries) HoldsReminder(ctx context.Context, payload []byte) (bool, error) {
	row := q.db.QueryRow(ctx, holdsReminder, payload)
	var held bool
	err := row.Scan(&held)
	return held, err
}

const holdsRetentionPolicy = `-- name: HoldsRetentionPolicy :one
SELECT EXISTS (SELECT 1 FROM retention_policy WHERE data_kind = ($1::jsonb->>'data_kind')::text) AS held
`

func (q *Queries) HoldsRetentionPolicy(ctx context.Context, payload []byte) (bool, error) {
	row := q.db.QueryRow(ctx, holdsRetentionPolicy, payload)
	var held bool
	err := row.Scan(&held)
	return held, err
}

const holdsSavedView = `-- name: HoldsSavedView :one
SELECT EXISTS (SELECT 1 FROM saved_view WHERE id = ($1::jsonb->>'id')::uuid) AS held
`

func (q *Queries) HoldsSavedView(ctx context.Context, payload []byte) (bool, error) {
	row := q.db.QueryRow(ctx, holdsSavedView, payload)
	var held bool
	err := row.Scan(&held)
	return held, err
}

const holdsSetElement = `-- name: HoldsSetElement :one
SELECT EXISTS (SELECT 1 FROM set_element WHERE item_id = ($1::jsonb->>'item_id')::uuid AND set_name = ($1::jsonb->>'set_name')::text AND element_id = ($1::jsonb->>'element_id')::uuid) AS held
`

func (q *Queries) HoldsSetElement(ctx context.Context, payload []byte) (bool, error) {
	row := q.db.QueryRow(ctx, holdsSetElement, payload)
	var held bool
	err := row.Scan(&held)
	return held, err
}

const holdsTemplate = `-- name: HoldsTemplate :one
SELECT EXISTS (SELECT 1 FROM template WHERE id = ($1::jsonb->>'id')::uuid) AS held
`

func (q *Queries) HoldsTemplate(ctx context.Context, payload []byte) (bool, error) {
	row := q.db.QueryRow(ctx, holdsTemplate, payload)
	var held bool
	err := row.Scan(&held)
	return held, err
}

const holdsTenant = `-- name: HoldsTenant :one
SELECT EXISTS (SELECT 1 FROM tenant WHERE id = ($1::jsonb->>'id')::uuid) AS held
`

func (q *Queries) HoldsTenant(ctx context.Context, payload []byte) (bool, error) {
	row := q.db.QueryRow(ctx, holdsTenant, payload)
	var held bool
	err := row.Scan(&held)
	return held, err
}

const holdsWebhookSubscription = `-- name: HoldsWebhookSubscription :one
SELECT EXISTS (SELECT 1 FROM webhook_subscription WHERE id = ($1::jsonb->>'id')::uuid) AS held
`

func (q *Queries) HoldsWebhookSubscription(ctx context.Context, payload []byte) (bool, error) {
	row := q.db.QueryRow(ctx, holdsWebhookSubscription, payload)
	var held bool
	err := row.Scan(&held)
	return held, err
}

const holdsWorkItem = `-- name: HoldsWorkItem :one
SELECT EXISTS (SELECT 1 FROM work_item WHERE id = ($1::jsonb->>'id')::uuid) AS held
`

func (q *Queries) HoldsWorkItem(ctx context.Context, payload []byte) (bool, error) {
	row := q.db.QueryRow(ctx, holdsWorkItem, payload)
	var held bool
	err := row.Scan(&held)
	return held, err
}

const importAccount = `-- name: ImportAccount :execrows
INSERT INTO account (
  id,
  tenant_id,
  kind,
  email,
  display_name,
  external_subject,
  password_hash,
  locale,
  time_zone,
  week_start,
  status,
  ai_consent,
  created_at,
  updated_at,
  deleted_at,
  version
)
SELECT
  r.id,
  r.tenant_id,
  r.kind,
  r.email,
  r.display_name,
  r.external_subject,
  r.password_hash,
  r.locale,
  r.time_zone,
  r.week_start,
  r.status,
  r.ai_consent,
  r.created_at,
  r.updated_at,
  r.deleted_at,
  r.version
FROM jsonb_populate_record(
  NULL::account,
  $1::jsonb || jsonb_build_object('tenant_id', current_tenant_id())
) r
ON CONFLICT (id) DO UPDATE SET
  tenant_id = EXCLUDED.tenant_id,
  kind = EXCLUDED.kind,
  email = EXCLUDED.email,
  display_name = EXCLUDED.display_name,
  external_subject = EXCLUDED.external_subject,
  password_hash = EXCLUDED.password_hash,
  locale = EXCLUDED.locale,
  time_zone = EXCLUDED.time_zone,
  week_start = EXCLUDED.week_start,
  status = EXCLUDED.status,
  ai_consent = EXCLUDED.ai_consent,
  created_at = EXCLUDED.created_at,
  updated_at = EXCLUDED.updated_at,
  deleted_at = EXCLUDED.deleted_at,
  version = EXCLUDED.version
WHERE $2::boolean
`

type ImportAccountParams struct {
	Payload   []byte
	Overwrite bool
}

func (q *Queries) ImportAccount(ctx context.Context, arg ImportAccountParams) (int64, error) {
	result, err := q.db.Exec(ctx, importAccount, arg.Payload, arg.Overwrite)
	if err != nil {
		return 0, err
	}
	return result.RowsAffected(), nil
}

const importAccountGroup = `-- name: ImportAccountGroup :execrows
INSERT INTO account_group (
  id,
  tenant_id,
  name,
  description,
  created_at,
  version
)
SELECT
  r.id,
  r.tenant_id,
  r.name,
  r.description,
  r.created_at,
  r.version
FROM jsonb_populate_record(
  NULL::account_group,
  $1::jsonb || jsonb_build_object('tenant_id', current_tenant_id())
) r
ON CONFLICT (id) DO UPDATE SET
  tenant_id = EXCLUDED.tenant_id,
  name = EXCLUDED.name,
  description = EXCLUDED.description,
  created_at = EXCLUDED.created_at,
  version = EXCLUDED.version
WHERE $2::boolean
`

type ImportAccountGroupParams struct {
	Payload   []byte
	Overwrite bool
}

func (q *Queries) ImportAccountGroup(ctx context.Context, arg ImportAccountGroupParams) (int64, error) {
	result, err := q.db.Exec(ctx, importAccountGroup, arg.Payload, arg.Overwrite)
	if err != nil {
		return 0, err
	}
	return result.RowsAffected(), nil
}

const importAccountGroupMember = `-- name: ImportAccountGroupMember :execrows
INSERT INTO account_group_member (
  tenant_id,
  group_id,
  account_id
)
SELECT
  r.tenant_id,
  r.group_id,
  r.account_id
FROM jsonb_populate_record(
  NULL::account_group_member,
  $1::jsonb || jsonb_build_object('tenant_id', current_tenant_id())
) r
ON CONFLICT (group_id, account_id) DO UPDATE SET
  tenant_id = EXCLUDED.tenant_id
WHERE $2::boolean
`

type ImportAccountGroupMemberParams struct {
	Payload   []byte
	Overwrite bool
}

func (q *Queries) ImportAccountGroupMember(ctx context.Context, arg ImportAccountGroupMemberParams) (int64, error) {
	result, err := q.db.Exec(ctx, importAccountGroupMember, arg.Payload, arg.Overwrite)
	if err != nil {
		return 0, err
	}
	return result.RowsAffected(), nil
}

const importActivityEntry = `-- name: ImportActivityEntry :execrows
INSERT INTO activity_entry (
  id,
  tenant_id,
  item_id,
  container_id,
  actor_type,
  actor_id,
  verb,
  change_set,
  occurred_at,
  correlation_id,
  causation_id
)
SELECT
  r.id,
  r.tenant_id,
  r.item_id,
  r.container_id,
  r.actor_type,
  r.actor_id,
  r.verb,
  r.change_set,
  r.occurred_at,
  r.correlation_id,
  r.causation_id
FROM jsonb_populate_record(
  NULL::activity_entry,
  $1::jsonb || jsonb_build_object('tenant_id', current_tenant_id())
) r
ON CONFLICT (tenant_id, occurred_at, id) DO UPDATE SET
  item_id = EXCLUDED.item_id,
  container_id = EXCLUDED.container_id,
  actor_type = EXCLUDED.actor_type,
  actor_id = EXCLUDED.actor_id,
  verb = EXCLUDED.verb,
  change_set = EXCLUDED.change_set,
  correlation_id = EXCLUDED.correlation_id,
  causation_id = EXCLUDED.causation_id
WHERE $2::boolean
`

type ImportActivityEntryParams struct {
	Payload   []byte
	Overwrite bool
}

// The conflict target is the partitioned key since H-09: a partitioned table cannot hold a
// unique index on id alone, and occurred_at is immutable on an activity entry, so the pair
// names the same row the old target did. tenant_id and occurred_at leave the update list -
// they are the key now.
func (q *Queries) ImportActivityEntry(ctx context.Context, arg ImportActivityEntryParams) (int64, error) {
	result, err := q.db.Exec(ctx, importActivityEntry, arg.Payload, arg.Overwrite)
	if err != nil {
		return 0, err
	}
	return result.RowsAffected(), nil
}

const importAutoAssignPolicy = `-- name: ImportAutoAssignPolicy :execrows
INSERT INTO auto_assign_policy (
  id,
  tenant_id,
  scope_type,
  scope_id,
  strategy,
  candidates,
  state,
  enabled,
  version
)
SELECT
  r.id,
  r.tenant_id,
  r.scope_type,
  r.scope_id,
  r.strategy,
  r.candidates,
  r.state,
  r.enabled,
  r.version
FROM jsonb_populate_record(
  NULL::auto_assign_policy,
  $1::jsonb || jsonb_build_object('tenant_id', current_tenant_id())
) r
ON CONFLICT (id) DO UPDATE SET
  tenant_id = EXCLUDED.tenant_id,
  scope_type = EXCLUDED.scope_type,
  scope_id = EXCLUDED.scope_id,
  strategy = EXCLUDED.strategy,
  candidates = EXCLUDED.candidates,
  state = EXCLUDED.state,
  enabled = EXCLUDED.enabled,
  version = EXCLUDED.version
WHERE $2::boolean
`

type ImportAutoAssignPolicyParams struct {
	Payload   []byte
	Overwrite bool
}

func (q *Queries) ImportAutoAssignPolicy(ctx context.Context, arg ImportAutoAssignPolicyParams) (int64, error) {
	result, err := q.db.Exec(ctx, importAutoAssignPolicy, arg.Payload, arg.Overwrite)
	if err != nil {
		return 0, err
	}
	return result.RowsAffected(), nil
}

const importAutomationRule = `-- name: ImportAutomationRule :execrows
INSERT INTO automation_rule (
  id,
  tenant_id,
  scope_type,
  scope_id,
  name,
  enabled,
  run_as,
  trigger,
  conditions,
  actions,
  throttle,
  on_error,
  failure_count,
  created_by,
  created_at,
  updated_at,
  deleted_at,
  version,
  -- Restored with the rule, so that a rule that comes back is the rule that went in. It fires
  -- nothing by itself: a restore seeds no poller, because nothing enumerates tenants, and
  -- backup-restore.md §8.4 is unambiguous that no rule acts because of a restore.
  next_run_at
)
SELECT
  r.id,
  r.tenant_id,
  r.scope_type,
  r.scope_id,
  r.name,
  r.enabled,
  r.run_as,
  r.trigger,
  r.conditions,
  r.actions,
  r.throttle,
  r.on_error,
  r.failure_count,
  r.created_by,
  r.created_at,
  r.updated_at,
  r.deleted_at,
  r.version,
  r.next_run_at
FROM jsonb_populate_record(
  NULL::automation_rule,
  $1::jsonb || jsonb_build_object('tenant_id', current_tenant_id())
) r
ON CONFLICT (id) DO UPDATE SET
  tenant_id = EXCLUDED.tenant_id,
  scope_type = EXCLUDED.scope_type,
  scope_id = EXCLUDED.scope_id,
  name = EXCLUDED.name,
  enabled = EXCLUDED.enabled,
  run_as = EXCLUDED.run_as,
  trigger = EXCLUDED.trigger,
  conditions = EXCLUDED.conditions,
  actions = EXCLUDED.actions,
  throttle = EXCLUDED.throttle,
  on_error = EXCLUDED.on_error,
  failure_count = EXCLUDED.failure_count,
  created_by = EXCLUDED.created_by,
  created_at = EXCLUDED.created_at,
  updated_at = EXCLUDED.updated_at,
  deleted_at = EXCLUDED.deleted_at,
  version = EXCLUDED.version,
  next_run_at = EXCLUDED.next_run_at
WHERE $2::boolean
`

type ImportAutomationRuleParams struct {
	Payload   []byte
	Overwrite bool
}

func (q *Queries) ImportAutomationRule(ctx context.Context, arg ImportAutomationRuleParams) (int64, error) {
	result, err := q.db.Exec(ctx, importAutomationRule, arg.Payload, arg.Overwrite)
	if err != nil {
		return 0, err
	}
	return result.RowsAffected(), nil
}

const importBucket = `-- name: ImportBucket :execrows
INSERT INTO bucket (
  id,
  tenant_id,
  collection_id,
  name,
  order_key,
  wip_limit,
  is_done_bucket,
  color_token,
  deleted_at,
  version
)
SELECT
  r.id,
  r.tenant_id,
  r.collection_id,
  r.name,
  r.order_key,
  r.wip_limit,
  r.is_done_bucket,
  r.color_token,
  r.deleted_at,
  r.version
FROM jsonb_populate_record(
  NULL::bucket,
  $1::jsonb || jsonb_build_object('tenant_id', current_tenant_id())
) r
ON CONFLICT (id) DO UPDATE SET
  tenant_id = EXCLUDED.tenant_id,
  collection_id = EXCLUDED.collection_id,
  name = EXCLUDED.name,
  order_key = EXCLUDED.order_key,
  wip_limit = EXCLUDED.wip_limit,
  is_done_bucket = EXCLUDED.is_done_bucket,
  color_token = EXCLUDED.color_token,
  deleted_at = EXCLUDED.deleted_at,
  version = EXCLUDED.version
WHERE $2::boolean
`

type ImportBucketParams struct {
	Payload   []byte
	Overwrite bool
}

func (q *Queries) ImportBucket(ctx context.Context, arg ImportBucketParams) (int64, error) {
	result, err := q.db.Exec(ctx, importBucket, arg.Payload, arg.Overwrite)
	if err != nil {
		return 0, err
	}
	return result.RowsAffected(), nil
}

const importCalendarFeed = `-- name: ImportCalendarFeed :execrows
INSERT INTO calendar_feed (
  id,
  tenant_id,
  account_id,
  view_id,
  token_hash,
  created_at,
  revoked_at
)
SELECT
  r.id,
  r.tenant_id,
  r.account_id,
  r.view_id,
  r.token_hash,
  r.created_at,
  r.revoked_at
FROM jsonb_populate_record(
  NULL::calendar_feed,
  $1::jsonb || jsonb_build_object('tenant_id', current_tenant_id())
) r
ON CONFLICT (id) DO UPDATE SET
  tenant_id = EXCLUDED.tenant_id,
  account_id = EXCLUDED.account_id,
  view_id = EXCLUDED.view_id,
  token_hash = EXCLUDED.token_hash,
  created_at = EXCLUDED.created_at,
  revoked_at = EXCLUDED.revoked_at
WHERE $2::boolean
`

type ImportCalendarFeedParams struct {
	Payload   []byte
	Overwrite bool
}

func (q *Queries) ImportCalendarFeed(ctx context.Context, arg ImportCalendarFeedParams) (int64, error) {
	result, err := q.db.Exec(ctx, importCalendarFeed, arg.Payload, arg.Overwrite)
	if err != nil {
		return 0, err
	}
	return result.RowsAffected(), nil
}

const importComment = `-- name: ImportComment :execrows
INSERT INTO comment (
  id,
  tenant_id,
  item_id,
  author_id,
  parent_comment_id,
  body,
  created_at,
  edited_at,
  deleted_at,
  version
)
SELECT
  r.id,
  r.tenant_id,
  r.item_id,
  r.author_id,
  r.parent_comment_id,
  r.body,
  r.created_at,
  r.edited_at,
  r.deleted_at,
  r.version
FROM jsonb_populate_record(
  NULL::comment,
  $1::jsonb || jsonb_build_object('tenant_id', current_tenant_id())
) r
ON CONFLICT (id) DO UPDATE SET
  tenant_id = EXCLUDED.tenant_id,
  item_id = EXCLUDED.item_id,
  author_id = EXCLUDED.author_id,
  parent_comment_id = EXCLUDED.parent_comment_id,
  body = EXCLUDED.body,
  created_at = EXCLUDED.created_at,
  edited_at = EXCLUDED.edited_at,
  deleted_at = EXCLUDED.deleted_at,
  version = EXCLUDED.version
WHERE $2::boolean
`

type ImportCommentParams struct {
	Payload   []byte
	Overwrite bool
}

func (q *Queries) ImportComment(ctx context.Context, arg ImportCommentParams) (int64, error) {
	result, err := q.db.Exec(ctx, importComment, arg.Payload, arg.Overwrite)
	if err != nil {
		return 0, err
	}
	return result.RowsAffected(), nil
}

const importConsentRecord = `-- name: ImportConsentRecord :execrows
INSERT INTO consent_record (
  id,
  tenant_id,
  account_id,
  purpose,
  granted,
  granted_at,
  revoked_at,
  source
)
SELECT
  r.id,
  r.tenant_id,
  r.account_id,
  r.purpose,
  r.granted,
  r.granted_at,
  r.revoked_at,
  r.source
FROM jsonb_populate_record(
  NULL::consent_record,
  $1::jsonb || jsonb_build_object('tenant_id', current_tenant_id())
) r
ON CONFLICT (id) DO UPDATE SET
  tenant_id = EXCLUDED.tenant_id,
  account_id = EXCLUDED.account_id,
  purpose = EXCLUDED.purpose,
  granted = EXCLUDED.granted,
  granted_at = EXCLUDED.granted_at,
  revoked_at = EXCLUDED.revoked_at,
  source = EXCLUDED.source
WHERE $2::boolean
`

type ImportConsentRecordParams struct {
	Payload   []byte
	Overwrite bool
}

func (q *Queries) ImportConsentRecord(ctx context.Context, arg ImportConsentRecordParams) (int64, error) {
	result, err := q.db.Exec(ctx, importConsentRecord, arg.Payload, arg.Overwrite)
	if err != nil {
		return 0, err
	}
	return result.RowsAffected(), nil
}

const importContainer = `-- name: ImportContainer :execrows
INSERT INTO container (
  id,
  tenant_id,
  type,
  parent_id,
  name,
  description,
  icon,
  color_token,
  order_key,
  policies,
  archived_at,
  deleted_at,
  trash_batch_id,
  created_by,
  created_at,
  updated_at,
  version
)
SELECT
  r.id,
  r.tenant_id,
  r.type,
  r.parent_id,
  r.name,
  r.description,
  r.icon,
  r.color_token,
  r.order_key,
  r.policies,
  r.archived_at,
  r.deleted_at,
  r.trash_batch_id,
  r.created_by,
  r.created_at,
  r.updated_at,
  r.version
FROM jsonb_populate_record(
  NULL::container,
  $1::jsonb || jsonb_build_object('tenant_id', current_tenant_id())
) r
ON CONFLICT (id) DO UPDATE SET
  tenant_id = EXCLUDED.tenant_id,
  type = EXCLUDED.type,
  parent_id = EXCLUDED.parent_id,
  name = EXCLUDED.name,
  description = EXCLUDED.description,
  icon = EXCLUDED.icon,
  color_token = EXCLUDED.color_token,
  order_key = EXCLUDED.order_key,
  policies = EXCLUDED.policies,
  archived_at = EXCLUDED.archived_at,
  deleted_at = EXCLUDED.deleted_at,
  trash_batch_id = EXCLUDED.trash_batch_id,
  created_by = EXCLUDED.created_by,
  created_at = EXCLUDED.created_at,
  updated_at = EXCLUDED.updated_at,
  version = EXCLUDED.version
WHERE $2::boolean
`

type ImportContainerParams struct {
	Payload   []byte
	Overwrite bool
}

func (q *Queries) ImportContainer(ctx context.Context, arg ImportContainerParams) (int64, error) {
	result, err := q.db.Exec(ctx, importContainer, arg.Payload, arg.Overwrite)
	if err != nil {
		return 0, err
	}
	return result.RowsAffected(), nil
}

const importCustomFieldDefinition = `-- name: ImportCustomFieldDefinition :execrows
INSERT INTO custom_field_definition (
  id,
  tenant_id,
  collection_id,
  key,
  kind,
  options,
  is_required,
  applies_to,
  deleted_at,
  created_at,
  updated_at,
  version
)
SELECT
  r.id,
  r.tenant_id,
  r.collection_id,
  r.key,
  r.kind,
  r.options,
  r.is_required,
  r.applies_to,
  r.deleted_at,
  r.created_at,
  r.updated_at,
  r.version
FROM jsonb_populate_record(
  NULL::custom_field_definition,
  $1::jsonb || jsonb_build_object('tenant_id', current_tenant_id())
) r
ON CONFLICT (id) DO UPDATE SET
  tenant_id = EXCLUDED.tenant_id,
  collection_id = EXCLUDED.collection_id,
  key = EXCLUDED.key,
  kind = EXCLUDED.kind,
  options = EXCLUDED.options,
  is_required = EXCLUDED.is_required,
  applies_to = EXCLUDED.applies_to,
  deleted_at = EXCLUDED.deleted_at,
  created_at = EXCLUDED.created_at,
  updated_at = EXCLUDED.updated_at,
  version = EXCLUDED.version
WHERE $2::boolean
`

type ImportCustomFieldDefinitionParams struct {
	Payload   []byte
	Overwrite bool
}

func (q *Queries) ImportCustomFieldDefinition(ctx context.Context, arg ImportCustomFieldDefinitionParams) (int64, error) {
	result, err := q.db.Exec(ctx, importCustomFieldDefinition, arg.Payload, arg.Overwrite)
	if err != nil {
		return 0, err
	}
	return result.RowsAffected(), nil
}

const importItemAttachment = `-- name: ImportItemAttachment :execrows
INSERT INTO item_attachment (
  tenant_id,
  item_id,
  media_id
)
SELECT
  r.tenant_id,
  r.item_id,
  r.media_id
FROM jsonb_populate_record(
  NULL::item_attachment,
  $1::jsonb || jsonb_build_object('tenant_id', current_tenant_id())
) r
ON CONFLICT (item_id, media_id) DO UPDATE SET
  tenant_id = EXCLUDED.tenant_id
WHERE $2::boolean
`

type ImportItemAttachmentParams struct {
	Payload   []byte
	Overwrite bool
}

func (q *Queries) ImportItemAttachment(ctx context.Context, arg ImportItemAttachmentParams) (int64, error) {
	result, err := q.db.Exec(ctx, importItemAttachment, arg.Payload, arg.Overwrite)
	if err != nil {
		return 0, err
	}
	return result.RowsAffected(), nil
}

const importItemLabel = `-- name: ImportItemLabel :execrows
INSERT INTO item_label (
  tenant_id,
  item_id,
  label_id
)
SELECT
  r.tenant_id,
  r.item_id,
  r.label_id
FROM jsonb_populate_record(
  NULL::item_label,
  $1::jsonb || jsonb_build_object('tenant_id', current_tenant_id())
) r
ON CONFLICT (item_id, label_id) DO UPDATE SET
  tenant_id = EXCLUDED.tenant_id
WHERE $2::boolean
`

type ImportItemLabelParams struct {
	Payload   []byte
	Overwrite bool
}

func (q *Queries) ImportItemLabel(ctx context.Context, arg ImportItemLabelParams) (int64, error) {
	result, err := q.db.Exec(ctx, importItemLabel, arg.Payload, arg.Overwrite)
	if err != nil {
		return 0, err
	}
	return result.RowsAffected(), nil
}

const importItemMember = `-- name: ImportItemMember :execrows
INSERT INTO item_member (
  tenant_id,
  item_id,
  account_id
)
SELECT
  r.tenant_id,
  r.item_id,
  r.account_id
FROM jsonb_populate_record(
  NULL::item_member,
  $1::jsonb || jsonb_build_object('tenant_id', current_tenant_id())
) r
ON CONFLICT (item_id, account_id) DO UPDATE SET
  tenant_id = EXCLUDED.tenant_id
WHERE $2::boolean
`

type ImportItemMemberParams struct {
	Payload   []byte
	Overwrite bool
}

func (q *Queries) ImportItemMember(ctx context.Context, arg ImportItemMemberParams) (int64, error) {
	result, err := q.db.Exec(ctx, importItemMember, arg.Payload, arg.Overwrite)
	if err != nil {
		return 0, err
	}
	return result.RowsAffected(), nil
}

const importJumbleEntry = `-- name: ImportJumbleEntry :execrows
INSERT INTO jumble_entry (
  id,
  tenant_id,
  channel,
  sender,
  raw_subject,
  raw_body,
  attachments,
  suggestion,
  status,
  target_item_id,
  received_at,
  processed_at
)
SELECT
  r.id,
  r.tenant_id,
  r.channel,
  r.sender,
  r.raw_subject,
  r.raw_body,
  r.attachments,
  r.suggestion,
  r.status,
  r.target_item_id,
  r.received_at,
  r.processed_at
FROM jsonb_populate_record(
  NULL::jumble_entry,
  $1::jsonb || jsonb_build_object('tenant_id', current_tenant_id())
) r
ON CONFLICT (id) DO UPDATE SET
  tenant_id = EXCLUDED.tenant_id,
  channel = EXCLUDED.channel,
  sender = EXCLUDED.sender,
  raw_subject = EXCLUDED.raw_subject,
  raw_body = EXCLUDED.raw_body,
  attachments = EXCLUDED.attachments,
  suggestion = EXCLUDED.suggestion,
  status = EXCLUDED.status,
  target_item_id = EXCLUDED.target_item_id,
  received_at = EXCLUDED.received_at,
  processed_at = EXCLUDED.processed_at
WHERE $2::boolean
`

type ImportJumbleEntryParams struct {
	Payload   []byte
	Overwrite bool
}

func (q *Queries) ImportJumbleEntry(ctx context.Context, arg ImportJumbleEntryParams) (int64, error) {
	result, err := q.db.Exec(ctx, importJumbleEntry, arg.Payload, arg.Overwrite)
	if err != nil {
		return 0, err
	}
	return result.RowsAffected(), nil
}

const importLabel = `-- name: ImportLabel :execrows
INSERT INTO label (
  id,
  tenant_id,
  collection_id,
  name,
  color_token,
  description,
  deleted_at,
  version
)
SELECT
  r.id,
  r.tenant_id,
  r.collection_id,
  r.name,
  r.color_token,
  r.description,
  r.deleted_at,
  r.version
FROM jsonb_populate_record(
  NULL::label,
  $1::jsonb || jsonb_build_object('tenant_id', current_tenant_id())
) r
ON CONFLICT (id) DO UPDATE SET
  tenant_id = EXCLUDED.tenant_id,
  collection_id = EXCLUDED.collection_id,
  name = EXCLUDED.name,
  color_token = EXCLUDED.color_token,
  description = EXCLUDED.description,
  deleted_at = EXCLUDED.deleted_at,
  version = EXCLUDED.version
WHERE $2::boolean
`

type ImportLabelParams struct {
	Payload   []byte
	Overwrite bool
}

func (q *Queries) ImportLabel(ctx context.Context, arg ImportLabelParams) (int64, error) {
	result, err := q.db.Exec(ctx, importLabel, arg.Payload, arg.Overwrite)
	if err != nil {
		return 0, err
	}
	return result.RowsAffected(), nil
}

const importLegalHold = `-- name: ImportLegalHold :execrows
INSERT INTO legal_hold (
  id,
  tenant_id,
  scope_kind,
  scope_id,
  reason,
  placed_by,
  placed_at,
  released_by,
  released_at
)
SELECT
  r.id,
  r.tenant_id,
  r.scope_kind,
  r.scope_id,
  r.reason,
  r.placed_by,
  r.placed_at,
  r.released_by,
  r.released_at
FROM jsonb_populate_record(
  NULL::legal_hold,
  $1::jsonb || jsonb_build_object('tenant_id', current_tenant_id())
) r
ON CONFLICT (id) DO UPDATE SET
  tenant_id = EXCLUDED.tenant_id,
  scope_kind = EXCLUDED.scope_kind,
  scope_id = EXCLUDED.scope_id,
  reason = EXCLUDED.reason,
  placed_by = EXCLUDED.placed_by,
  placed_at = EXCLUDED.placed_at,
  released_by = EXCLUDED.released_by,
  released_at = EXCLUDED.released_at
WHERE $2::boolean
`

type ImportLegalHoldParams struct {
	Payload   []byte
	Overwrite bool
}

func (q *Queries) ImportLegalHold(ctx context.Context, arg ImportLegalHoldParams) (int64, error) {
	result, err := q.db.Exec(ctx, importLegalHold, arg.Payload, arg.Overwrite)
	if err != nil {
		return 0, err
	}
	return result.RowsAffected(), nil
}

const importMediaObject = `-- name: ImportMediaObject :execrows
INSERT INTO media_object (
  id,
  tenant_id,
  storage_key,
  mime_type,
  byte_size,
  checksum,
  usage,
  ref_count,
  created_by,
  created_at,
  deleted_at,
  status,
  file_name
)
SELECT
  r.id,
  r.tenant_id,
  r.storage_key,
  r.mime_type,
  r.byte_size,
  r.checksum,
  r.usage,
  r.ref_count,
  r.created_by,
  r.created_at,
  r.deleted_at,
  r.status,
  r.file_name
FROM jsonb_populate_record(
  NULL::media_object,
  $1::jsonb || jsonb_build_object('tenant_id', current_tenant_id())
) r
ON CONFLICT (id) DO UPDATE SET
  tenant_id = EXCLUDED.tenant_id,
  storage_key = EXCLUDED.storage_key,
  mime_type = EXCLUDED.mime_type,
  byte_size = EXCLUDED.byte_size,
  checksum = EXCLUDED.checksum,
  usage = EXCLUDED.usage,
  ref_count = EXCLUDED.ref_count,
  created_by = EXCLUDED.created_by,
  created_at = EXCLUDED.created_at,
  deleted_at = EXCLUDED.deleted_at,
  status = EXCLUDED.status,
  file_name = EXCLUDED.file_name
WHERE $2::boolean
`

type ImportMediaObjectParams struct {
	Payload   []byte
	Overwrite bool
}

func (q *Queries) ImportMediaObject(ctx context.Context, arg ImportMediaObjectParams) (int64, error) {
	result, err := q.db.Exec(ctx, importMediaObject, arg.Payload, arg.Overwrite)
	if err != nil {
		return 0, err
	}
	return result.RowsAffected(), nil
}

const importMembership = `-- name: ImportMembership :execrows
INSERT INTO membership (
  id,
  tenant_id,
  account_id,
  group_id,
  scope_type,
  scope_id,
  role,
  created_at
)
SELECT
  r.id,
  r.tenant_id,
  r.account_id,
  r.group_id,
  r.scope_type,
  r.scope_id,
  r.role,
  r.created_at
FROM jsonb_populate_record(
  NULL::membership,
  $1::jsonb || jsonb_build_object('tenant_id', current_tenant_id())
) r
ON CONFLICT (id) DO UPDATE SET
  tenant_id = EXCLUDED.tenant_id,
  account_id = EXCLUDED.account_id,
  group_id = EXCLUDED.group_id,
  scope_type = EXCLUDED.scope_type,
  scope_id = EXCLUDED.scope_id,
  role = EXCLUDED.role,
  created_at = EXCLUDED.created_at
WHERE $2::boolean
`

type ImportMembershipParams struct {
	Payload   []byte
	Overwrite bool
}

func (q *Queries) ImportMembership(ctx context.Context, arg ImportMembershipParams) (int64, error) {
	result, err := q.db.Exec(ctx, importMembership, arg.Payload, arg.Overwrite)
	if err != nil {
		return 0, err
	}
	return result.RowsAffected(), nil
}

const importNotificationPreference = `-- name: ImportNotificationPreference :execrows
INSERT INTO notification_preference (
  tenant_id,
  account_id,
  category,
  channel,
  enabled,
  include_title,
  updated_at
)
SELECT
  r.tenant_id,
  r.account_id,
  r.category,
  r.channel,
  r.enabled,
  r.include_title,
  r.updated_at
FROM jsonb_populate_record(
  NULL::notification_preference,
  $1::jsonb || jsonb_build_object('tenant_id', current_tenant_id())
) r
ON CONFLICT (tenant_id, account_id, category, channel) DO UPDATE SET
  enabled = EXCLUDED.enabled,
  include_title = EXCLUDED.include_title,
  updated_at = EXCLUDED.updated_at
WHERE $2::boolean
`

type ImportNotificationPreferenceParams struct {
	Payload   []byte
	Overwrite bool
}

func (q *Queries) ImportNotificationPreference(ctx context.Context, arg ImportNotificationPreferenceParams) (int64, error) {
	result, err := q.db.Exec(ctx, importNotificationPreference, arg.Payload, arg.Overwrite)
	if err != nil {
		return 0, err
	}
	return result.RowsAffected(), nil
}

const importRecurrenceRule = `-- name: ImportRecurrenceRule :execrows
INSERT INTO recurrence_rule (
  id,
  tenant_id,
  source_item_id,
  rrule,
  time_zone,
  mode,
  horizon_days,
  ends_at,
  max_count,
  last_materialized_at,
  created_at,
  version,
  updated_at
)
SELECT
  r.id,
  r.tenant_id,
  r.source_item_id,
  r.rrule,
  r.time_zone,
  r.mode,
  r.horizon_days,
  r.ends_at,
  r.max_count,
  r.last_materialized_at,
  r.created_at,
  r.version,
  r.updated_at
FROM jsonb_populate_record(
  NULL::recurrence_rule,
  $1::jsonb || jsonb_build_object('tenant_id', current_tenant_id())
) r
ON CONFLICT (id) DO UPDATE SET
  tenant_id = EXCLUDED.tenant_id,
  source_item_id = EXCLUDED.source_item_id,
  rrule = EXCLUDED.rrule,
  time_zone = EXCLUDED.time_zone,
  mode = EXCLUDED.mode,
  horizon_days = EXCLUDED.horizon_days,
  ends_at = EXCLUDED.ends_at,
  max_count = EXCLUDED.max_count,
  last_materialized_at = EXCLUDED.last_materialized_at,
  created_at = EXCLUDED.created_at,
  version = EXCLUDED.version,
  updated_at = EXCLUDED.updated_at
WHERE $2::boolean
`

type ImportRecurrenceRuleParams struct {
	Payload   []byte
	Overwrite bool
}

func (q *Queries) ImportRecurrenceRule(ctx context.Context, arg ImportRecurrenceRuleParams) (int64, error) {
	result, err := q.db.Exec(ctx, importRecurrenceRule, arg.Payload, arg.Overwrite)
	if err != nil {
		return 0, err
	}
	return result.RowsAffected(), nil
}

const importReminder = `-- name: ImportReminder :execrows
INSERT INTO reminder (
  id,
  tenant_id,
  item_id,
  offset_spec,
  channels,
  recipients,
  state,
  fire_at,
  created_at,
  version,
  updated_at
)
SELECT
  r.id,
  r.tenant_id,
  r.item_id,
  r.offset_spec,
  r.channels,
  r.recipients,
  r.state,
  r.fire_at,
  r.created_at,
  r.version,
  r.updated_at
FROM jsonb_populate_record(
  NULL::reminder,
  $1::jsonb || jsonb_build_object('tenant_id', current_tenant_id())
) r
ON CONFLICT (id) DO UPDATE SET
  tenant_id = EXCLUDED.tenant_id,
  item_id = EXCLUDED.item_id,
  offset_spec = EXCLUDED.offset_spec,
  channels = EXCLUDED.channels,
  recipients = EXCLUDED.recipients,
  state = EXCLUDED.state,
  fire_at = EXCLUDED.fire_at,
  created_at = EXCLUDED.created_at,
  version = EXCLUDED.version,
  updated_at = EXCLUDED.updated_at
WHERE $2::boolean
`

type ImportReminderParams struct {
	Payload   []byte
	Overwrite bool
}

func (q *Queries) ImportReminder(ctx context.Context, arg ImportReminderParams) (int64, error) {
	result, err := q.db.Exec(ctx, importReminder, arg.Payload, arg.Overwrite)
	if err != nil {
		return 0, err
	}
	return result.RowsAffected(), nil
}

const importRetentionPolicy = `-- name: ImportRetentionPolicy :execrows
INSERT INTO retention_policy (
  tenant_id,
  data_kind,
  retain_days,
  min_days,
  max_days,
  justification,
  updated_at,
  updated_by
)
SELECT
  r.tenant_id,
  r.data_kind,
  r.retain_days,
  r.min_days,
  r.max_days,
  r.justification,
  r.updated_at,
  r.updated_by
FROM jsonb_populate_record(
  NULL::retention_policy,
  $1::jsonb || jsonb_build_object('tenant_id', current_tenant_id())
) r
ON CONFLICT (tenant_id, data_kind) DO UPDATE SET
  retain_days = EXCLUDED.retain_days,
  min_days = EXCLUDED.min_days,
  max_days = EXCLUDED.max_days,
  justification = EXCLUDED.justification,
  updated_at = EXCLUDED.updated_at,
  updated_by = EXCLUDED.updated_by
WHERE $2::boolean
`

type ImportRetentionPolicyParams struct {
	Payload   []byte
	Overwrite bool
}

func (q *Queries) ImportRetentionPolicy(ctx context.Context, arg ImportRetentionPolicyParams) (int64, error) {
	result, err := q.db.Exec(ctx, importRetentionPolicy, arg.Payload, arg.Overwrite)
	if err != nil {
		return 0, err
	}
	return result.RowsAffected(), nil
}

const importSavedView = `-- name: ImportSavedView :execrows
INSERT INTO saved_view (
  id,
  tenant_id,
  scope_type,
  scope_id,
  owner_id,
  name,
  layout,
  query,
  grouping,
  visible_fields,
  sharing,
  created_at,
  version
)
SELECT
  r.id,
  r.tenant_id,
  r.scope_type,
  r.scope_id,
  r.owner_id,
  r.name,
  r.layout,
  r.query,
  r.grouping,
  r.visible_fields,
  r.sharing,
  r.created_at,
  r.version
FROM jsonb_populate_record(
  NULL::saved_view,
  $1::jsonb || jsonb_build_object('tenant_id', current_tenant_id())
) r
ON CONFLICT (id) DO UPDATE SET
  tenant_id = EXCLUDED.tenant_id,
  scope_type = EXCLUDED.scope_type,
  scope_id = EXCLUDED.scope_id,
  owner_id = EXCLUDED.owner_id,
  name = EXCLUDED.name,
  layout = EXCLUDED.layout,
  query = EXCLUDED.query,
  grouping = EXCLUDED.grouping,
  visible_fields = EXCLUDED.visible_fields,
  sharing = EXCLUDED.sharing,
  created_at = EXCLUDED.created_at,
  version = EXCLUDED.version
WHERE $2::boolean
`

type ImportSavedViewParams struct {
	Payload   []byte
	Overwrite bool
}

func (q *Queries) ImportSavedView(ctx context.Context, arg ImportSavedViewParams) (int64, error) {
	result, err := q.db.Exec(ctx, importSavedView, arg.Payload, arg.Overwrite)
	if err != nil {
		return 0, err
	}
	return result.RowsAffected(), nil
}

const importSetElement = `-- name: ImportSetElement :execrows
INSERT INTO set_element (
  tenant_id,
  item_id,
  set_name,
  element_id,
  add_tag,
  remove_tag
)
SELECT
  r.tenant_id,
  r.item_id,
  r.set_name,
  r.element_id,
  r.add_tag,
  r.remove_tag
FROM jsonb_populate_record(
  NULL::set_element,
  $1::jsonb || jsonb_build_object('tenant_id', current_tenant_id())
) r
ON CONFLICT (tenant_id, item_id, set_name, element_id) DO UPDATE SET
  add_tag = EXCLUDED.add_tag,
  remove_tag = EXCLUDED.remove_tag
WHERE $2::boolean
`

type ImportSetElementParams struct {
	Payload   []byte
	Overwrite bool
}

func (q *Queries) ImportSetElement(ctx context.Context, arg ImportSetElementParams) (int64, error) {
	result, err := q.db.Exec(ctx, importSetElement, arg.Payload, arg.Overwrite)
	if err != nil {
		return 0, err
	}
	return result.RowsAffected(), nil
}

const importTemplate = `-- name: ImportTemplate :execrows
INSERT INTO template (
  id,
  tenant_id,
  scope_type,
  scope_id,
  name,
  description,
  root_type,
  nodes,
  created_at,
  deleted_at,
  version,
  updated_at
)
SELECT
  r.id,
  r.tenant_id,
  r.scope_type,
  r.scope_id,
  r.name,
  r.description,
  r.root_type,
  r.nodes,
  r.created_at,
  r.deleted_at,
  r.version,
  r.updated_at
FROM jsonb_populate_record(
  NULL::template,
  $1::jsonb || jsonb_build_object('tenant_id', current_tenant_id())
) r
ON CONFLICT (id) DO UPDATE SET
  tenant_id = EXCLUDED.tenant_id,
  scope_type = EXCLUDED.scope_type,
  scope_id = EXCLUDED.scope_id,
  name = EXCLUDED.name,
  description = EXCLUDED.description,
  root_type = EXCLUDED.root_type,
  nodes = EXCLUDED.nodes,
  created_at = EXCLUDED.created_at,
  deleted_at = EXCLUDED.deleted_at,
  version = EXCLUDED.version,
  updated_at = EXCLUDED.updated_at
WHERE $2::boolean
`

type ImportTemplateParams struct {
	Payload   []byte
	Overwrite bool
}

func (q *Queries) ImportTemplate(ctx context.Context, arg ImportTemplateParams) (int64, error) {
	result, err := q.db.Exec(ctx, importTemplate, arg.Payload, arg.Overwrite)
	if err != nil {
		return 0, err
	}
	return result.RowsAffected(), nil
}

const importTenant = `-- name: ImportTenant :execrows

INSERT INTO tenant (
  id,
  slug,
  display_name,
  status,
  default_locale,
  default_time_zone,
  settings,
  created_at,
  updated_at,
  deleted_at,
  version
)
SELECT
  r.id,
  r.slug,
  r.display_name,
  r.status,
  r.default_locale,
  r.default_time_zone,
  r.settings,
  r.created_at,
  r.updated_at,
  r.deleted_at,
  r.version
FROM jsonb_populate_record(
  NULL::tenant,
  $1::jsonb || jsonb_build_object('tenant_id', current_tenant_id())
) r
ON CONFLICT (id) DO UPDATE SET
  slug = EXCLUDED.slug,
  display_name = EXCLUDED.display_name,
  status = EXCLUDED.status,
  default_locale = EXCLUDED.default_locale,
  default_time_zone = EXCLUDED.default_time_zone,
  settings = EXCLUDED.settings,
  created_at = EXCLUDED.created_at,
  updated_at = EXCLUDED.updated_at,
  deleted_at = EXCLUDED.deleted_at,
  version = EXCLUDED.version
WHERE $2::boolean
`

type ImportTenantParams struct {
	Payload   []byte
	Overwrite bool
}

// The statements a restore writes a tenant through (E-06, backup-restore.md §8).
//
// One trio per entity: does the tenant already hold this row, write it, and - for REPLACE_TENANT -
// empty the table. They are as alike as the schema lets them be, and they are hand-written for the
// reason the export's are: what differs between them is a decision per entity - which columns
// identify a row, and which of them a generated column may not be written into - and a generator
// would have to be told all of that anyway.
//
// **The row arrives as one jsonb value and is unpacked by the database.** `jsonb_populate_record`
// against the table's own row type is what makes the import a statement rather than a string with
// thirty-eight parameters in it, and rule 9 is the reason that matters: no byte of an archive ever
// becomes SQL text. A field the archive carries and the table does not have is dropped by the
// unpacking; a column the archive does not carry arrives NULL.
//
// `tenant_id` is not taken from the archive. It is `current_tenant_id()`, the same value row level
// security compares against, so a restore cannot write into another tenant even deliberately -
// which is BK-10 at the layer where it cannot be forgotten (ADR-0010).
//
// The upsert's `WHERE @overwrite` is the conflict rule, in the statement rather than in a branch
// above it: SKIP is the same statement with the flag false, and a row that was neither inserted nor
// updated returns nothing at all, which is how the caller tells the three outcomes apart.
func (q *Queries) ImportTenant(ctx context.Context, arg ImportTenantParams) (int64, error) {
	result, err := q.db.Exec(ctx, importTenant, arg.Payload, arg.Overwrite)
	if err != nil {
		return 0, err
	}
	return result.RowsAffected(), nil
}

const importWebhookSubscription = `-- name: ImportWebhookSubscription :execrows
INSERT INTO webhook_subscription (
  id,
  tenant_id,
  target_url,
  event_types,
  filter_expr,
  secret_enc,
  state,
  failure_count,
  created_by,
  created_at,
  version
)
SELECT
  r.id,
  r.tenant_id,
  r.target_url,
  r.event_types,
  r.filter_expr,
  r.secret_enc,
  r.state,
  r.failure_count,
  r.created_by,
  r.created_at,
  r.version
FROM jsonb_populate_record(
  NULL::webhook_subscription,
  $1::jsonb || jsonb_build_object('tenant_id', current_tenant_id())
) r
ON CONFLICT (id) DO UPDATE SET
  tenant_id = EXCLUDED.tenant_id,
  target_url = EXCLUDED.target_url,
  event_types = EXCLUDED.event_types,
  filter_expr = EXCLUDED.filter_expr,
  secret_enc = EXCLUDED.secret_enc,
  state = EXCLUDED.state,
  failure_count = EXCLUDED.failure_count,
  created_by = EXCLUDED.created_by,
  created_at = EXCLUDED.created_at,
  version = EXCLUDED.version
WHERE $2::boolean
`

type ImportWebhookSubscriptionParams struct {
	Payload   []byte
	Overwrite bool
}

func (q *Queries) ImportWebhookSubscription(ctx context.Context, arg ImportWebhookSubscriptionParams) (int64, error) {
	result, err := q.db.Exec(ctx, importWebhookSubscription, arg.Payload, arg.Overwrite)
	if err != nil {
		return 0, err
	}
	return result.RowsAffected(), nil
}

const importWorkItem = `-- name: ImportWorkItem :execrows
INSERT INTO work_item (
  id,
  tenant_id,
  collection_id,
  type,
  parent_id,
  path,
  depth,
  title,
  notes,
  is_completed,
  completed_at,
  completed_by,
  bucket_id,
  order_key,
  start_at,
  due_at,
  due_date_only,
  due_time_zone,
  assignee_id,
  cover_kind,
  cover_color_token,
  cover_media_id,
  custom_fields,
  recurrence_rule_id,
  origin_jumble_id,
  content_language,
  archived_at,
  deleted_at,
  trash_batch_id,
  created_by,
  created_at,
  updated_at,
  version,
  custom_field_refs,
  search_document,
  due_soon_announced_at,
  overdue_announced_at,
  retention_pending_until,
  retention_rule_id,
  retention_action,
  retention_blocked_by
)
SELECT
  r.id,
  r.tenant_id,
  r.collection_id,
  r.type,
  r.parent_id,
  r.path,
  r.depth,
  r.title,
  r.notes,
  r.is_completed,
  r.completed_at,
  r.completed_by,
  r.bucket_id,
  r.order_key,
  r.start_at,
  r.due_at,
  r.due_date_only,
  r.due_time_zone,
  r.assignee_id,
  r.cover_kind,
  r.cover_color_token,
  r.cover_media_id,
  r.custom_fields,
  r.recurrence_rule_id,
  r.origin_jumble_id,
  r.content_language,
  r.archived_at,
  r.deleted_at,
  r.trash_batch_id,
  r.created_by,
  r.created_at,
  r.updated_at,
  r.version,
  r.custom_field_refs,
  r.search_document,
  r.due_soon_announced_at,
  r.overdue_announced_at,
  r.retention_pending_until,
  r.retention_rule_id,
  r.retention_action,
  r.retention_blocked_by
FROM jsonb_populate_record(
  NULL::work_item,
  $1::jsonb || jsonb_build_object('tenant_id', current_tenant_id())
) r
ON CONFLICT (id) DO UPDATE SET
  tenant_id = EXCLUDED.tenant_id,
  collection_id = EXCLUDED.collection_id,
  type = EXCLUDED.type,
  parent_id = EXCLUDED.parent_id,
  path = EXCLUDED.path,
  depth = EXCLUDED.depth,
  title = EXCLUDED.title,
  notes = EXCLUDED.notes,
  is_completed = EXCLUDED.is_completed,
  completed_at = EXCLUDED.completed_at,
  completed_by = EXCLUDED.completed_by,
  bucket_id = EXCLUDED.bucket_id,
  order_key = EXCLUDED.order_key,
  start_at = EXCLUDED.start_at,
  due_at = EXCLUDED.due_at,
  due_date_only = EXCLUDED.due_date_only,
  due_time_zone = EXCLUDED.due_time_zone,
  assignee_id = EXCLUDED.assignee_id,
  cover_kind = EXCLUDED.cover_kind,
  cover_color_token = EXCLUDED.cover_color_token,
  cover_media_id = EXCLUDED.cover_media_id,
  custom_fields = EXCLUDED.custom_fields,
  recurrence_rule_id = EXCLUDED.recurrence_rule_id,
  origin_jumble_id = EXCLUDED.origin_jumble_id,
  content_language = EXCLUDED.content_language,
  archived_at = EXCLUDED.archived_at,
  deleted_at = EXCLUDED.deleted_at,
  trash_batch_id = EXCLUDED.trash_batch_id,
  created_by = EXCLUDED.created_by,
  created_at = EXCLUDED.created_at,
  updated_at = EXCLUDED.updated_at,
  version = EXCLUDED.version,
  custom_field_refs = EXCLUDED.custom_field_refs,
  search_document = EXCLUDED.search_document,
  due_soon_announced_at = EXCLUDED.due_soon_announced_at,
  overdue_announced_at = EXCLUDED.overdue_announced_at,
  retention_pending_until = EXCLUDED.retention_pending_until,
  retention_rule_id = EXCLUDED.retention_rule_id,
  retention_action = EXCLUDED.retention_action,
  retention_blocked_by = EXCLUDED.retention_blocked_by
WHERE $2::boolean
`

type ImportWorkItemParams struct {
	Payload   []byte
	Overwrite bool
}

// `search_vector` is generated by the database and is therefore not written back: the archive carries
// what the column held, and PostgreSQL refuses an insert into one.
func (q *Queries) ImportWorkItem(ctx context.Context, arg ImportWorkItemParams) (int64, error) {
	result, err := q.db.Exec(ctx, importWorkItem, arg.Payload, arg.Overwrite)
	if err != nil {
		return 0, err
	}
	return result.RowsAffected(), nil
}

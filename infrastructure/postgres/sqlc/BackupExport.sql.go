// Code generated by sqlc. DO NOT EDIT.
// versions:
//   sqlc v1.31.1
// source: BackupExport.sql

package sqlc

import (
	"context"

	"github.com/jackc/pgx/v5/pgtype"
)

const exportAccountGroupMembers = `-- name: ExportAccountGroupMembers :many
SELECT s.record_id, s.changed_at, s.payload
FROM (
  SELECT (m.group_id::text || '/' || m.account_id::text)::text AS record_id,
         NULL::timestamptz AS changed_at,
         (to_jsonb(m) - 'tenant_id')::jsonb AS payload,
         m.group_id AS key_1,
         m.account_id AS key_2
  FROM account_group_member m
) s
  WHERE (s.key_1, s.key_2) > ($1::uuid, $2::uuid)
  ORDER BY s.key_1, s.key_2
  LIMIT $3::int
`

type ExportAccountGroupMembersParams struct {
	AfterID     pgtype.UUID
	AfterSecond pgtype.UUID
	Batch       int32
}

type ExportAccountGroupMembersRow struct {
	RecordID  string
	ChangedAt pgtype.Timestamptz
	Payload   []byte
}

func (q *Queries) ExportAccountGroupMembers(ctx context.Context, arg ExportAccountGroupMembersParams) ([]ExportAccountGroupMembersRow, error) {
	rows, err := q.db.Query(ctx, exportAccountGroupMembers, arg.AfterID, arg.AfterSecond, arg.Batch)
	if err != nil {
		return nil, err
	}
	defer rows.Close()
	items := []ExportAccountGroupMembersRow{}
	for rows.Next() {
		var i ExportAccountGroupMembersRow
		if err := rows.Scan(&i.RecordID, &i.ChangedAt, &i.Payload); err != nil {
			return nil, err
		}
		items = append(items, i)
	}
	if err := rows.Err(); err != nil {
		return nil, err
	}
	return items, nil
}

const exportAccountGroups = `-- name: ExportAccountGroups :many
SELECT s.record_id, s.changed_at, s.payload
FROM (
  SELECT (g.id)::text AS record_id,
         NULL::timestamptz AS changed_at,
         (to_jsonb(g) - 'tenant_id')::jsonb AS payload,
         g.id AS key_1
  FROM account_group g
) s
  WHERE s.key_1 > $1::uuid
  ORDER BY s.key_1
  LIMIT $2::int
`

type ExportAccountGroupsParams struct {
	AfterID pgtype.UUID
	Batch   int32
}

type ExportAccountGroupsRow struct {
	RecordID  string
	ChangedAt pgtype.Timestamptz
	Payload   []byte
}

func (q *Queries) ExportAccountGroups(ctx context.Context, arg ExportAccountGroupsParams) ([]ExportAccountGroupsRow, error) {
	rows, err := q.db.Query(ctx, exportAccountGroups, arg.AfterID, arg.Batch)
	if err != nil {
		return nil, err
	}
	defer rows.Close()
	items := []ExportAccountGroupsRow{}
	for rows.Next() {
		var i ExportAccountGroupsRow
		if err := rows.Scan(&i.RecordID, &i.ChangedAt, &i.Payload); err != nil {
			return nil, err
		}
		items = append(items, i)
	}
	if err := rows.Err(); err != nil {
		return nil, err
	}
	return items, nil
}

const exportAccounts = `-- name: ExportAccounts :many
SELECT s.record_id, s.changed_at, s.payload
FROM (
  SELECT (a.id)::text AS record_id,
         a.updated_at AS changed_at,
         (to_jsonb(a) - 'tenant_id')::jsonb AS payload,
         a.id AS key_1
  FROM account a
) s
  WHERE ($1::timestamptz IS NULL OR s.changed_at > $1::timestamptz)
    AND (s.changed_at, s.key_1) > ($2::timestamptz, $3::uuid)
  ORDER BY s.changed_at, s.key_1
  LIMIT $4::int
`

type ExportAccountsParams struct {
	Since   pgtype.Timestamptz
	AfterAt pgtype.Timestamptz
	AfterID pgtype.UUID
	Batch   int32
}

type ExportAccountsRow struct {
	RecordID  string
	ChangedAt pgtype.Timestamptz
	Payload   []byte
}

func (q *Queries) ExportAccounts(ctx context.Context, arg ExportAccountsParams) ([]ExportAccountsRow, error) {
	rows, err := q.db.Query(ctx, exportAccounts,
		arg.Since,
		arg.AfterAt,
		arg.AfterID,
		arg.Batch,
	)
	if err != nil {
		return nil, err
	}
	defer rows.Close()
	items := []ExportAccountsRow{}
	for rows.Next() {
		var i ExportAccountsRow
		if err := rows.Scan(&i.RecordID, &i.ChangedAt, &i.Payload); err != nil {
			return nil, err
		}
		items = append(items, i)
	}
	if err := rows.Err(); err != nil {
		return nil, err
	}
	return items, nil
}

const exportActivityEntries = `-- name: ExportActivityEntries :many
SELECT s.record_id, s.changed_at, s.payload
FROM (
  SELECT (a.id)::text AS record_id,
         a.occurred_at AS changed_at,
         (to_jsonb(a) - 'tenant_id')::jsonb AS payload,
         a.id AS key_1
  FROM activity_entry a
) s
  WHERE ($1::timestamptz IS NULL OR s.changed_at > $1::timestamptz)
    AND (s.changed_at, s.key_1) > ($2::timestamptz, $3::uuid)
  ORDER BY s.changed_at, s.key_1
  LIMIT $4::int
`

type ExportActivityEntriesParams struct {
	Since   pgtype.Timestamptz
	AfterAt pgtype.Timestamptz
	AfterID pgtype.UUID
	Batch   int32
}

type ExportActivityEntriesRow struct {
	RecordID  string
	ChangedAt pgtype.Timestamptz
	Payload   []byte
}

func (q *Queries) ExportActivityEntries(ctx context.Context, arg ExportActivityEntriesParams) ([]ExportActivityEntriesRow, error) {
	rows, err := q.db.Query(ctx, exportActivityEntries,
		arg.Since,
		arg.AfterAt,
		arg.AfterID,
		arg.Batch,
	)
	if err != nil {
		return nil, err
	}
	defer rows.Close()
	items := []ExportActivityEntriesRow{}
	for rows.Next() {
		var i ExportActivityEntriesRow
		if err := rows.Scan(&i.RecordID, &i.ChangedAt, &i.Payload); err != nil {
			return nil, err
		}
		items = append(items, i)
	}
	if err := rows.Err(); err != nil {
		return nil, err
	}
	return items, nil
}

const exportAudit = `-- name: ExportAudit :many
SELECT s.record_id, s.changed_at, s.payload
FROM (
  SELECT (a.seq::text)::text AS record_id,
         a.occurred_at AS changed_at,
         (to_jsonb(a) - 'tenant_id')::jsonb AS payload,
         a.seq AS key_1
  FROM audit_log a
) s
  WHERE ($1::timestamptz IS NULL OR s.changed_at > $1::timestamptz)
    AND (s.changed_at, s.key_1) > ($2::timestamptz, $3::bigint)
  ORDER BY s.changed_at, s.key_1
  LIMIT $4::int
`

type ExportAuditParams struct {
	Since   pgtype.Timestamptz
	AfterAt pgtype.Timestamptz
	AfterID int64
	Batch   int32
}

type ExportAuditRow struct {
	RecordID  string
	ChangedAt pgtype.Timestamptz
	Payload   []byte
}

func (q *Queries) ExportAudit(ctx context.Context, arg ExportAuditParams) ([]ExportAuditRow, error) {
	rows, err := q.db.Query(ctx, exportAudit,
		arg.Since,
		arg.AfterAt,
		arg.AfterID,
		arg.Batch,
	)
	if err != nil {
		return nil, err
	}
	defer rows.Close()
	items := []ExportAuditRow{}
	for rows.Next() {
		var i ExportAuditRow
		if err := rows.Scan(&i.RecordID, &i.ChangedAt, &i.Payload); err != nil {
			return nil, err
		}
		items = append(items, i)
	}
	if err := rows.Err(); err != nil {
		return nil, err
	}
	return items, nil
}

const exportAutoAssignPolicies = `-- name: ExportAutoAssignPolicies :many
SELECT s.record_id, s.changed_at, s.payload
FROM (
  SELECT (p.id)::text AS record_id,
         NULL::timestamptz AS changed_at,
         (to_jsonb(p) - 'tenant_id')::jsonb AS payload,
         p.id AS key_1
  FROM auto_assign_policy p
) s
  WHERE s.key_1 > $1::uuid
  ORDER BY s.key_1
  LIMIT $2::int
`

type ExportAutoAssignPoliciesParams struct {
	AfterID pgtype.UUID
	Batch   int32
}

type ExportAutoAssignPoliciesRow struct {
	RecordID  string
	ChangedAt pgtype.Timestamptz
	Payload   []byte
}

func (q *Queries) ExportAutoAssignPolicies(ctx context.Context, arg ExportAutoAssignPoliciesParams) ([]ExportAutoAssignPoliciesRow, error) {
	rows, err := q.db.Query(ctx, exportAutoAssignPolicies, arg.AfterID, arg.Batch)
	if err != nil {
		return nil, err
	}
	defer rows.Close()
	items := []ExportAutoAssignPoliciesRow{}
	for rows.Next() {
		var i ExportAutoAssignPoliciesRow
		if err := rows.Scan(&i.RecordID, &i.ChangedAt, &i.Payload); err != nil {
			return nil, err
		}
		items = append(items, i)
	}
	if err := rows.Err(); err != nil {
		return nil, err
	}
	return items, nil
}

const exportAutomationRules = `-- name: ExportAutomationRules :many
SELECT s.record_id, s.changed_at, s.payload
FROM (
  SELECT (r.id)::text AS record_id,
         r.updated_at AS changed_at,
         (to_jsonb(r) - 'tenant_id')::jsonb AS payload,
         r.id AS key_1
  FROM automation_rule r
) s
  WHERE ($1::timestamptz IS NULL OR s.changed_at > $1::timestamptz)
    AND (s.changed_at, s.key_1) > ($2::timestamptz, $3::uuid)
  ORDER BY s.changed_at, s.key_1
  LIMIT $4::int
`

type ExportAutomationRulesParams struct {
	Since   pgtype.Timestamptz
	AfterAt pgtype.Timestamptz
	AfterID pgtype.UUID
	Batch   int32
}

type ExportAutomationRulesRow struct {
	RecordID  string
	ChangedAt pgtype.Timestamptz
	Payload   []byte
}

func (q *Queries) ExportAutomationRules(ctx context.Context, arg ExportAutomationRulesParams) ([]ExportAutomationRulesRow, error) {
	rows, err := q.db.Query(ctx, exportAutomationRules,
		arg.Since,
		arg.AfterAt,
		arg.AfterID,
		arg.Batch,
	)
	if err != nil {
		return nil, err
	}
	defer rows.Close()
	items := []ExportAutomationRulesRow{}
	for rows.Next() {
		var i ExportAutomationRulesRow
		if err := rows.Scan(&i.RecordID, &i.ChangedAt, &i.Payload); err != nil {
			return nil, err
		}
		items = append(items, i)
	}
	if err := rows.Err(); err != nil {
		return nil, err
	}
	return items, nil
}

const exportBuckets = `-- name: ExportBuckets :many
SELECT s.record_id, s.changed_at, s.payload
FROM (
  SELECT (b.id)::text AS record_id,
         NULL::timestamptz AS changed_at,
         (to_jsonb(b) - 'tenant_id')::jsonb AS payload,
         b.id AS key_1
  FROM bucket b
) s
  WHERE s.key_1 > $1::uuid
  ORDER BY s.key_1
  LIMIT $2::int
`

type ExportBucketsParams struct {
	AfterID pgtype.UUID
	Batch   int32
}

type ExportBucketsRow struct {
	RecordID  string
	ChangedAt pgtype.Timestamptz
	Payload   []byte
}

func (q *Queries) ExportBuckets(ctx context.Context, arg ExportBucketsParams) ([]ExportBucketsRow, error) {
	rows, err := q.db.Query(ctx, exportBuckets, arg.AfterID, arg.Batch)
	if err != nil {
		return nil, err
	}
	defer rows.Close()
	items := []ExportBucketsRow{}
	for rows.Next() {
		var i ExportBucketsRow
		if err := rows.Scan(&i.RecordID, &i.ChangedAt, &i.Payload); err != nil {
			return nil, err
		}
		items = append(items, i)
	}
	if err := rows.Err(); err != nil {
		return nil, err
	}
	return items, nil
}

const exportCalendarFeeds = `-- name: ExportCalendarFeeds :many
SELECT s.record_id, s.changed_at, s.payload
FROM (
  SELECT (f.id)::text AS record_id,
         NULL::timestamptz AS changed_at,
         (to_jsonb(f) - 'tenant_id')::jsonb AS payload,
         f.id AS key_1
  FROM calendar_feed f
) s
  WHERE s.key_1 > $1::uuid
  ORDER BY s.key_1
  LIMIT $2::int
`

type ExportCalendarFeedsParams struct {
	AfterID pgtype.UUID
	Batch   int32
}

type ExportCalendarFeedsRow struct {
	RecordID  string
	ChangedAt pgtype.Timestamptz
	Payload   []byte
}

func (q *Queries) ExportCalendarFeeds(ctx context.Context, arg ExportCalendarFeedsParams) ([]ExportCalendarFeedsRow, error) {
	rows, err := q.db.Query(ctx, exportCalendarFeeds, arg.AfterID, arg.Batch)
	if err != nil {
		return nil, err
	}
	defer rows.Close()
	items := []ExportCalendarFeedsRow{}
	for rows.Next() {
		var i ExportCalendarFeedsRow
		if err := rows.Scan(&i.RecordID, &i.ChangedAt, &i.Payload); err != nil {
			return nil, err
		}
		items = append(items, i)
	}
	if err := rows.Err(); err != nil {
		return nil, err
	}
	return items, nil
}

const exportComments = `-- name: ExportComments :many
SELECT s.record_id, s.changed_at, s.payload
FROM (
  SELECT (c.id)::text AS record_id,
         GREATEST(c.created_at, COALESCE(c.edited_at, c.created_at), COALESCE(c.deleted_at, c.created_at))::timestamptz AS changed_at,
         (to_jsonb(c) - 'tenant_id')::jsonb AS payload,
         c.id AS key_1
  FROM comment c
) s
  WHERE ($1::timestamptz IS NULL OR s.changed_at > $1::timestamptz)
    AND (s.changed_at, s.key_1) > ($2::timestamptz, $3::uuid)
  ORDER BY s.changed_at, s.key_1
  LIMIT $4::int
`

type ExportCommentsParams struct {
	Since   pgtype.Timestamptz
	AfterAt pgtype.Timestamptz
	AfterID pgtype.UUID
	Batch   int32
}

type ExportCommentsRow struct {
	RecordID  string
	ChangedAt pgtype.Timestamptz
	Payload   []byte
}

func (q *Queries) ExportComments(ctx context.Context, arg ExportCommentsParams) ([]ExportCommentsRow, error) {
	rows, err := q.db.Query(ctx, exportComments,
		arg.Since,
		arg.AfterAt,
		arg.AfterID,
		arg.Batch,
	)
	if err != nil {
		return nil, err
	}
	defer rows.Close()
	items := []ExportCommentsRow{}
	for rows.Next() {
		var i ExportCommentsRow
		if err := rows.Scan(&i.RecordID, &i.ChangedAt, &i.Payload); err != nil {
			return nil, err
		}
		items = append(items, i)
	}
	if err := rows.Err(); err != nil {
		return nil, err
	}
	return items, nil
}

const exportConsentRecords = `-- name: ExportConsentRecords :many
SELECT s.record_id, s.changed_at, s.payload
FROM (
  SELECT (c.id)::text AS record_id,
         NULL::timestamptz AS changed_at,
         (to_jsonb(c) - 'tenant_id')::jsonb AS payload,
         c.id AS key_1
  FROM consent_record c
) s
  WHERE s.key_1 > $1::uuid
  ORDER BY s.key_1
  LIMIT $2::int
`

type ExportConsentRecordsParams struct {
	AfterID pgtype.UUID
	Batch   int32
}

type ExportConsentRecordsRow struct {
	RecordID  string
	ChangedAt pgtype.Timestamptz
	Payload   []byte
}

func (q *Queries) ExportConsentRecords(ctx context.Context, arg ExportConsentRecordsParams) ([]ExportConsentRecordsRow, error) {
	rows, err := q.db.Query(ctx, exportConsentRecords, arg.AfterID, arg.Batch)
	if err != nil {
		return nil, err
	}
	defer rows.Close()
	items := []ExportConsentRecordsRow{}
	for rows.Next() {
		var i ExportConsentRecordsRow
		if err := rows.Scan(&i.RecordID, &i.ChangedAt, &i.Payload); err != nil {
			return nil, err
		}
		items = append(items, i)
	}
	if err := rows.Err(); err != nil {
		return nil, err
	}
	return items, nil
}

const exportContainers = `-- name: ExportContainers :many
SELECT s.record_id, s.changed_at, s.payload
FROM (
  SELECT (c.id)::text AS record_id,
         c.updated_at AS changed_at,
         (to_jsonb(c) - 'tenant_id')::jsonb AS payload,
         c.id AS key_1
  FROM container c
) s
  WHERE ($1::timestamptz IS NULL OR s.changed_at > $1::timestamptz)
    AND (s.changed_at, s.key_1) > ($2::timestamptz, $3::uuid)
  ORDER BY s.changed_at, s.key_1
  LIMIT $4::int
`

type ExportContainersParams struct {
	Since   pgtype.Timestamptz
	AfterAt pgtype.Timestamptz
	AfterID pgtype.UUID
	Batch   int32
}

type ExportContainersRow struct {
	RecordID  string
	ChangedAt pgtype.Timestamptz
	Payload   []byte
}

func (q *Queries) ExportContainers(ctx context.Context, arg ExportContainersParams) ([]ExportContainersRow, error) {
	rows, err := q.db.Query(ctx, exportContainers,
		arg.Since,
		arg.AfterAt,
		arg.AfterID,
		arg.Batch,
	)
	if err != nil {
		return nil, err
	}
	defer rows.Close()
	items := []ExportContainersRow{}
	for rows.Next() {
		var i ExportContainersRow
		if err := rows.Scan(&i.RecordID, &i.ChangedAt, &i.Payload); err != nil {
			return nil, err
		}
		items = append(items, i)
	}
	if err := rows.Err(); err != nil {
		return nil, err
	}
	return items, nil
}

const exportCustomFieldDefinitions = `-- name: ExportCustomFieldDefinitions :many
SELECT s.record_id, s.changed_at, s.payload
FROM (
  SELECT (f.id)::text AS record_id,
         f.updated_at AS changed_at,
         (to_jsonb(f) - 'tenant_id')::jsonb AS payload,
         f.id AS key_1
  FROM custom_field_definition f
) s
  WHERE ($1::timestamptz IS NULL OR s.changed_at > $1::timestamptz)
    AND (s.changed_at, s.key_1) > ($2::timestamptz, $3::uuid)
  ORDER BY s.changed_at, s.key_1
  LIMIT $4::int
`

type ExportCustomFieldDefinitionsParams struct {
	Since   pgtype.Timestamptz
	AfterAt pgtype.Timestamptz
	AfterID pgtype.UUID
	Batch   int32
}

type ExportCustomFieldDefinitionsRow struct {
	RecordID  string
	ChangedAt pgtype.Timestamptz
	Payload   []byte
}

func (q *Queries) ExportCustomFieldDefinitions(ctx context.Context, arg ExportCustomFieldDefinitionsParams) ([]ExportCustomFieldDefinitionsRow, error) {
	rows, err := q.db.Query(ctx, exportCustomFieldDefinitions,
		arg.Since,
		arg.AfterAt,
		arg.AfterID,
		arg.Batch,
	)
	if err != nil {
		return nil, err
	}
	defer rows.Close()
	items := []ExportCustomFieldDefinitionsRow{}
	for rows.Next() {
		var i ExportCustomFieldDefinitionsRow
		if err := rows.Scan(&i.RecordID, &i.ChangedAt, &i.Payload); err != nil {
			return nil, err
		}
		items = append(items, i)
	}
	if err := rows.Err(); err != nil {
		return nil, err
	}
	return items, nil
}

const exportItemAttachments = `-- name: ExportItemAttachments :many
SELECT s.record_id, s.changed_at, s.payload
FROM (
  SELECT (ia.item_id::text || '/' || ia.media_id::text)::text AS record_id,
         NULL::timestamptz AS changed_at,
         (to_jsonb(ia) - 'tenant_id')::jsonb AS payload,
         ia.item_id AS key_1,
         ia.media_id AS key_2
  FROM item_attachment ia
) s
  WHERE (s.key_1, s.key_2) > ($1::uuid, $2::uuid)
  ORDER BY s.key_1, s.key_2
  LIMIT $3::int
`

type ExportItemAttachmentsParams struct {
	AfterID     pgtype.UUID
	AfterSecond pgtype.UUID
	Batch       int32
}

type ExportItemAttachmentsRow struct {
	RecordID  string
	ChangedAt pgtype.Timestamptz
	Payload   []byte
}

func (q *Queries) ExportItemAttachments(ctx context.Context, arg ExportItemAttachmentsParams) ([]ExportItemAttachmentsRow, error) {
	rows, err := q.db.Query(ctx, exportItemAttachments, arg.AfterID, arg.AfterSecond, arg.Batch)
	if err != nil {
		return nil, err
	}
	defer rows.Close()
	items := []ExportItemAttachmentsRow{}
	for rows.Next() {
		var i ExportItemAttachmentsRow
		if err := rows.Scan(&i.RecordID, &i.ChangedAt, &i.Payload); err != nil {
			return nil, err
		}
		items = append(items, i)
	}
	if err := rows.Err(); err != nil {
		return nil, err
	}
	return items, nil
}

const exportItemLabels = `-- name: ExportItemLabels :many
SELECT s.record_id, s.changed_at, s.payload
FROM (
  SELECT (il.item_id::text || '/' || il.label_id::text)::text AS record_id,
         NULL::timestamptz AS changed_at,
         (to_jsonb(il) - 'tenant_id')::jsonb AS payload,
         il.item_id AS key_1,
         il.label_id AS key_2
  FROM item_label il
) s
  WHERE (s.key_1, s.key_2) > ($1::uuid, $2::uuid)
  ORDER BY s.key_1, s.key_2
  LIMIT $3::int
`

type ExportItemLabelsParams struct {
	AfterID     pgtype.UUID
	AfterSecond pgtype.UUID
	Batch       int32
}

type ExportItemLabelsRow struct {
	RecordID  string
	ChangedAt pgtype.Timestamptz
	Payload   []byte
}

func (q *Queries) ExportItemLabels(ctx context.Context, arg ExportItemLabelsParams) ([]ExportItemLabelsRow, error) {
	rows, err := q.db.Query(ctx, exportItemLabels, arg.AfterID, arg.AfterSecond, arg.Batch)
	if err != nil {
		return nil, err
	}
	defer rows.Close()
	items := []ExportItemLabelsRow{}
	for rows.Next() {
		var i ExportItemLabelsRow
		if err := rows.Scan(&i.RecordID, &i.ChangedAt, &i.Payload); err != nil {
			return nil, err
		}
		items = append(items, i)
	}
	if err := rows.Err(); err != nil {
		return nil, err
	}
	return items, nil
}

const exportItemMembers = `-- name: ExportItemMembers :many
SELECT s.record_id, s.changed_at, s.payload
FROM (
  SELECT (im.item_id::text || '/' || im.account_id::text)::text AS record_id,
         NULL::timestamptz AS changed_at,
         (to_jsonb(im) - 'tenant_id')::jsonb AS payload,
         im.item_id AS key_1,
         im.account_id AS key_2
  FROM item_member im
) s
  WHERE (s.key_1, s.key_2) > ($1::uuid, $2::uuid)
  ORDER BY s.key_1, s.key_2
  LIMIT $3::int
`

type ExportItemMembersParams struct {
	AfterID     pgtype.UUID
	AfterSecond pgtype.UUID
	Batch       int32
}

type ExportItemMembersRow struct {
	RecordID  string
	ChangedAt pgtype.Timestamptz
	Payload   []byte
}

func (q *Queries) ExportItemMembers(ctx context.Context, arg ExportItemMembersParams) ([]ExportItemMembersRow, error) {
	rows, err := q.db.Query(ctx, exportItemMembers, arg.AfterID, arg.AfterSecond, arg.Batch)
	if err != nil {
		return nil, err
	}
	defer rows.Close()
	items := []ExportItemMembersRow{}
	for rows.Next() {
		var i ExportItemMembersRow
		if err := rows.Scan(&i.RecordID, &i.ChangedAt, &i.Payload); err != nil {
			return nil, err
		}
		items = append(items, i)
	}
	if err := rows.Err(); err != nil {
		return nil, err
	}
	return items, nil
}

const exportJumbleEntries = `-- name: ExportJumbleEntries :many
SELECT s.record_id, s.changed_at, s.payload
FROM (
  SELECT (j.id)::text AS record_id,
         GREATEST(j.received_at, COALESCE(j.processed_at, j.received_at))::timestamptz AS changed_at,
         (to_jsonb(j) - 'tenant_id')::jsonb AS payload,
         j.id AS key_1
  FROM jumble_entry j
) s
  WHERE ($1::timestamptz IS NULL OR s.changed_at > $1::timestamptz)
    AND (s.changed_at, s.key_1) > ($2::timestamptz, $3::uuid)
  ORDER BY s.changed_at, s.key_1
  LIMIT $4::int
`

type ExportJumbleEntriesParams struct {
	Since   pgtype.Timestamptz
	AfterAt pgtype.Timestamptz
	AfterID pgtype.UUID
	Batch   int32
}

type ExportJumbleEntriesRow struct {
	RecordID  string
	ChangedAt pgtype.Timestamptz
	Payload   []byte
}

func (q *Queries) ExportJumbleEntries(ctx context.Context, arg ExportJumbleEntriesParams) ([]ExportJumbleEntriesRow, error) {
	rows, err := q.db.Query(ctx, exportJumbleEntries,
		arg.Since,
		arg.AfterAt,
		arg.AfterID,
		arg.Batch,
	)
	if err != nil {
		return nil, err
	}
	defer rows.Close()
	items := []ExportJumbleEntriesRow{}
	for rows.Next() {
		var i ExportJumbleEntriesRow
		if err := rows.Scan(&i.RecordID, &i.ChangedAt, &i.Payload); err != nil {
			return nil, err
		}
		items = append(items, i)
	}
	if err := rows.Err(); err != nil {
		return nil, err
	}
	return items, nil
}

const exportLabels = `-- name: ExportLabels :many
SELECT s.record_id, s.changed_at, s.payload
FROM (
  SELECT (l.id)::text AS record_id,
         NULL::timestamptz AS changed_at,
         (to_jsonb(l) - 'tenant_id')::jsonb AS payload,
         l.id AS key_1
  FROM label l
) s
  WHERE s.key_1 > $1::uuid
  ORDER BY s.key_1
  LIMIT $2::int
`

type ExportLabelsParams struct {
	AfterID pgtype.UUID
	Batch   int32
}

type ExportLabelsRow struct {
	RecordID  string
	ChangedAt pgtype.Timestamptz
	Payload   []byte
}

func (q *Queries) ExportLabels(ctx context.Context, arg ExportLabelsParams) ([]ExportLabelsRow, error) {
	rows, err := q.db.Query(ctx, exportLabels, arg.AfterID, arg.Batch)
	if err != nil {
		return nil, err
	}
	defer rows.Close()
	items := []ExportLabelsRow{}
	for rows.Next() {
		var i ExportLabelsRow
		if err := rows.Scan(&i.RecordID, &i.ChangedAt, &i.Payload); err != nil {
			return nil, err
		}
		items = append(items, i)
	}
	if err := rows.Err(); err != nil {
		return nil, err
	}
	return items, nil
}

const exportLegalHolds = `-- name: ExportLegalHolds :many
SELECT s.record_id, s.changed_at, s.payload
FROM (
  SELECT (h.id)::text AS record_id,
         NULL::timestamptz AS changed_at,
         (to_jsonb(h) - 'tenant_id')::jsonb AS payload,
         h.id AS key_1
  FROM legal_hold h
) s
  WHERE s.key_1 > $1::uuid
  ORDER BY s.key_1
  LIMIT $2::int
`

type ExportLegalHoldsParams struct {
	AfterID pgtype.UUID
	Batch   int32
}

type ExportLegalHoldsRow struct {
	RecordID  string
	ChangedAt pgtype.Timestamptz
	Payload   []byte
}

func (q *Queries) ExportLegalHolds(ctx context.Context, arg ExportLegalHoldsParams) ([]ExportLegalHoldsRow, error) {
	rows, err := q.db.Query(ctx, exportLegalHolds, arg.AfterID, arg.Batch)
	if err != nil {
		return nil, err
	}
	defer rows.Close()
	items := []ExportLegalHoldsRow{}
	for rows.Next() {
		var i ExportLegalHoldsRow
		if err := rows.Scan(&i.RecordID, &i.ChangedAt, &i.Payload); err != nil {
			return nil, err
		}
		items = append(items, i)
	}
	if err := rows.Err(); err != nil {
		return nil, err
	}
	return items, nil
}

const exportMediaObjects = `-- name: ExportMediaObjects :many
SELECT s.record_id, s.changed_at, s.payload
FROM (
  SELECT (m.id)::text AS record_id,
         NULL::timestamptz AS changed_at,
         (to_jsonb(m) - 'tenant_id')::jsonb AS payload,
         m.id AS key_1
  FROM media_object m
) s
  WHERE s.key_1 > $1::uuid
  ORDER BY s.key_1
  LIMIT $2::int
`

type ExportMediaObjectsParams struct {
	AfterID pgtype.UUID
	Batch   int32
}

type ExportMediaObjectsRow struct {
	RecordID  string
	ChangedAt pgtype.Timestamptz
	Payload   []byte
}

func (q *Queries) ExportMediaObjects(ctx context.Context, arg ExportMediaObjectsParams) ([]ExportMediaObjectsRow, error) {
	rows, err := q.db.Query(ctx, exportMediaObjects, arg.AfterID, arg.Batch)
	if err != nil {
		return nil, err
	}
	defer rows.Close()
	items := []ExportMediaObjectsRow{}
	for rows.Next() {
		var i ExportMediaObjectsRow
		if err := rows.Scan(&i.RecordID, &i.ChangedAt, &i.Payload); err != nil {
			return nil, err
		}
		items = append(items, i)
	}
	if err := rows.Err(); err != nil {
		return nil, err
	}
	return items, nil
}

const exportMemberships = `-- name: ExportMemberships :many
SELECT s.record_id, s.changed_at, s.payload
FROM (
  SELECT (m.id)::text AS record_id,
         NULL::timestamptz AS changed_at,
         (to_jsonb(m) - 'tenant_id')::jsonb AS payload,
         m.id AS key_1
  FROM membership m
) s
  WHERE s.key_1 > $1::uuid
  ORDER BY s.key_1
  LIMIT $2::int
`

type ExportMembershipsParams struct {
	AfterID pgtype.UUID
	Batch   int32
}

type ExportMembershipsRow struct {
	RecordID  string
	ChangedAt pgtype.Timestamptz
	Payload   []byte
}

func (q *Queries) ExportMemberships(ctx context.Context, arg ExportMembershipsParams) ([]ExportMembershipsRow, error) {
	rows, err := q.db.Query(ctx, exportMemberships, arg.AfterID, arg.Batch)
	if err != nil {
		return nil, err
	}
	defer rows.Close()
	items := []ExportMembershipsRow{}
	for rows.Next() {
		var i ExportMembershipsRow
		if err := rows.Scan(&i.RecordID, &i.ChangedAt, &i.Payload); err != nil {
			return nil, err
		}
		items = append(items, i)
	}
	if err := rows.Err(); err != nil {
		return nil, err
	}
	return items, nil
}

const exportNotificationPreferences = `-- name: ExportNotificationPreferences :many
SELECT s.record_id, s.changed_at, s.payload
FROM (
  SELECT (p.account_id::text || '/' || p.category || '/' || p.channel)::text AS record_id,
         p.updated_at AS changed_at,
         (to_jsonb(p) - 'tenant_id')::jsonb AS payload,
         p.account_id AS key_1,
         p.category AS key_2,
         p.channel AS key_3
  FROM notification_preference p
) s
  WHERE ($1::timestamptz IS NULL OR s.changed_at > $1::timestamptz)
    AND (s.changed_at, s.key_1, s.key_2, s.key_3) > ($2::timestamptz, $3::uuid, $4::text, $5::text)
  ORDER BY s.changed_at, s.key_1, s.key_2, s.key_3
  LIMIT $6::int
`

type ExportNotificationPreferencesParams struct {
	Since       pgtype.Timestamptz
	AfterAt     pgtype.Timestamptz
	AfterID     pgtype.UUID
	AfterSecond string
	AfterThird  string
	Batch       int32
}

type ExportNotificationPreferencesRow struct {
	RecordID  string
	ChangedAt pgtype.Timestamptz
	Payload   []byte
}

func (q *Queries) ExportNotificationPreferences(ctx context.Context, arg ExportNotificationPreferencesParams) ([]ExportNotificationPreferencesRow, error) {
	rows, err := q.db.Query(ctx, exportNotificationPreferences,
		arg.Since,
		arg.AfterAt,
		arg.AfterID,
		arg.AfterSecond,
		arg.AfterThird,
		arg.Batch,
	)
	if err != nil {
		return nil, err
	}
	defer rows.Close()
	items := []ExportNotificationPreferencesRow{}
	for rows.Next() {
		var i ExportNotificationPreferencesRow
		if err := rows.Scan(&i.RecordID, &i.ChangedAt, &i.Payload); err != nil {
			return nil, err
		}
		items = append(items, i)
	}
	if err := rows.Err(); err != nil {
		return nil, err
	}
	return items, nil
}

const exportRecurrenceRules = `-- name: ExportRecurrenceRules :many
SELECT s.record_id, s.changed_at, s.payload
FROM (
  SELECT (r.id)::text AS record_id,
         r.updated_at AS changed_at,
         (to_jsonb(r) - 'tenant_id')::jsonb AS payload,
         r.id AS key_1
  FROM recurrence_rule r
) s
  WHERE ($1::timestamptz IS NULL OR s.changed_at > $1::timestamptz)
    AND (s.changed_at, s.key_1) > ($2::timestamptz, $3::uuid)
  ORDER BY s.changed_at, s.key_1
  LIMIT $4::int
`

type ExportRecurrenceRulesParams struct {
	Since   pgtype.Timestamptz
	AfterAt pgtype.Timestamptz
	AfterID pgtype.UUID
	Batch   int32
}

type ExportRecurrenceRulesRow struct {
	RecordID  string
	ChangedAt pgtype.Timestamptz
	Payload   []byte
}

func (q *Queries) ExportRecurrenceRules(ctx context.Context, arg ExportRecurrenceRulesParams) ([]ExportRecurrenceRulesRow, error) {
	rows, err := q.db.Query(ctx, exportRecurrenceRules,
		arg.Since,
		arg.AfterAt,
		arg.AfterID,
		arg.Batch,
	)
	if err != nil {
		return nil, err
	}
	defer rows.Close()
	items := []ExportRecurrenceRulesRow{}
	for rows.Next() {
		var i ExportRecurrenceRulesRow
		if err := rows.Scan(&i.RecordID, &i.ChangedAt, &i.Payload); err != nil {
			return nil, err
		}
		items = append(items, i)
	}
	if err := rows.Err(); err != nil {
		return nil, err
	}
	return items, nil
}

const exportReminders = `-- name: ExportReminders :many
SELECT s.record_id, s.changed_at, s.payload
FROM (
  SELECT (r.id)::text AS record_id,
         r.updated_at AS changed_at,
         (to_jsonb(r) - 'tenant_id')::jsonb AS payload,
         r.id AS key_1
  FROM reminder r
) s
  WHERE ($1::timestamptz IS NULL OR s.changed_at > $1::timestamptz)
    AND (s.changed_at, s.key_1) > ($2::timestamptz, $3::uuid)
  ORDER BY s.changed_at, s.key_1
  LIMIT $4::int
`

type ExportRemindersParams struct {
	Since   pgtype.Timestamptz
	AfterAt pgtype.Timestamptz
	AfterID pgtype.UUID
	Batch   int32
}

type ExportRemindersRow struct {
	RecordID  string
	ChangedAt pgtype.Timestamptz
	Payload   []byte
}

func (q *Queries) ExportReminders(ctx context.Context, arg ExportRemindersParams) ([]ExportRemindersRow, error) {
	rows, err := q.db.Query(ctx, exportReminders,
		arg.Since,
		arg.AfterAt,
		arg.AfterID,
		arg.Batch,
	)
	if err != nil {
		return nil, err
	}
	defer rows.Close()
	items := []ExportRemindersRow{}
	for rows.Next() {
		var i ExportRemindersRow
		if err := rows.Scan(&i.RecordID, &i.ChangedAt, &i.Payload); err != nil {
			return nil, err
		}
		items = append(items, i)
	}
	if err := rows.Err(); err != nil {
		return nil, err
	}
	return items, nil
}

const exportRetentionPolicies = `-- name: ExportRetentionPolicies :many
SELECT s.record_id, s.changed_at, s.payload
FROM (
  SELECT (p.data_kind)::text AS record_id,
         p.updated_at AS changed_at,
         (to_jsonb(p) - 'tenant_id')::jsonb AS payload,
         p.data_kind AS key_1
  FROM retention_policy p
) s
  WHERE ($1::timestamptz IS NULL OR s.changed_at > $1::timestamptz)
    AND (s.changed_at, s.key_1) > ($2::timestamptz, $3::text)
  ORDER BY s.changed_at, s.key_1
  LIMIT $4::int
`

type ExportRetentionPoliciesParams struct {
	Since   pgtype.Timestamptz
	AfterAt pgtype.Timestamptz
	AfterID string
	Batch   int32
}

type ExportRetentionPoliciesRow struct {
	RecordID  string
	ChangedAt pgtype.Timestamptz
	Payload   []byte
}

func (q *Queries) ExportRetentionPolicies(ctx context.Context, arg ExportRetentionPoliciesParams) ([]ExportRetentionPoliciesRow, error) {
	rows, err := q.db.Query(ctx, exportRetentionPolicies,
		arg.Since,
		arg.AfterAt,
		arg.AfterID,
		arg.Batch,
	)
	if err != nil {
		return nil, err
	}
	defer rows.Close()
	items := []ExportRetentionPoliciesRow{}
	for rows.Next() {
		var i ExportRetentionPoliciesRow
		if err := rows.Scan(&i.RecordID, &i.ChangedAt, &i.Payload); err != nil {
			return nil, err
		}
		items = append(items, i)
	}
	if err := rows.Err(); err != nil {
		return nil, err
	}
	return items, nil
}

const exportSavedViews = `-- name: ExportSavedViews :many
SELECT s.record_id, s.changed_at, s.payload
FROM (
  SELECT (v.id)::text AS record_id,
         NULL::timestamptz AS changed_at,
         (to_jsonb(v) - 'tenant_id')::jsonb AS payload,
         v.id AS key_1
  FROM saved_view v
) s
  WHERE s.key_1 > $1::uuid
  ORDER BY s.key_1
  LIMIT $2::int
`

type ExportSavedViewsParams struct {
	AfterID pgtype.UUID
	Batch   int32
}

type ExportSavedViewsRow struct {
	RecordID  string
	ChangedAt pgtype.Timestamptz
	Payload   []byte
}

func (q *Queries) ExportSavedViews(ctx context.Context, arg ExportSavedViewsParams) ([]ExportSavedViewsRow, error) {
	rows, err := q.db.Query(ctx, exportSavedViews, arg.AfterID, arg.Batch)
	if err != nil {
		return nil, err
	}
	defer rows.Close()
	items := []ExportSavedViewsRow{}
	for rows.Next() {
		var i ExportSavedViewsRow
		if err := rows.Scan(&i.RecordID, &i.ChangedAt, &i.Payload); err != nil {
			return nil, err
		}
		items = append(items, i)
	}
	if err := rows.Err(); err != nil {
		return nil, err
	}
	return items, nil
}

const exportSetElements = `-- name: ExportSetElements :many
SELECT s.record_id, s.changed_at, s.payload
FROM (
  SELECT (e.item_id::text || '/' || e.set_name || '/' || e.element_id::text)::text AS record_id,
         NULL::timestamptz AS changed_at,
         (to_jsonb(e) - 'tenant_id')::jsonb AS payload,
         e.item_id AS key_1,
         e.set_name AS key_2,
         e.element_id AS key_3
  FROM set_element e
) s
  WHERE (s.key_1, s.key_2, s.key_3) > ($1::uuid, $2::text, $3::uuid)
  ORDER BY s.key_1, s.key_2, s.key_3
  LIMIT $4::int
`

type ExportSetElementsParams struct {
	AfterID     pgtype.UUID
	AfterSecond string
	AfterThird  pgtype.UUID
	Batch       int32
}

type ExportSetElementsRow struct {
	RecordID  string
	ChangedAt pgtype.Timestamptz
	Payload   []byte
}

func (q *Queries) ExportSetElements(ctx context.Context, arg ExportSetElementsParams) ([]ExportSetElementsRow, error) {
	rows, err := q.db.Query(ctx, exportSetElements,
		arg.AfterID,
		arg.AfterSecond,
		arg.AfterThird,
		arg.Batch,
	)
	if err != nil {
		return nil, err
	}
	defer rows.Close()
	items := []ExportSetElementsRow{}
	for rows.Next() {
		var i ExportSetElementsRow
		if err := rows.Scan(&i.RecordID, &i.ChangedAt, &i.Payload); err != nil {
			return nil, err
		}
		items = append(items, i)
	}
	if err := rows.Err(); err != nil {
		return nil, err
	}
	return items, nil
}

const exportTemplates = `-- name: ExportTemplates :many
SELECT s.record_id, s.changed_at, s.payload
FROM (
  SELECT (t.id)::text AS record_id,
         t.updated_at AS changed_at,
         (to_jsonb(t) - 'tenant_id')::jsonb AS payload,
         t.id AS key_1
  FROM template t
) s
  WHERE ($1::timestamptz IS NULL OR s.changed_at > $1::timestamptz)
    AND (s.changed_at, s.key_1) > ($2::timestamptz, $3::uuid)
  ORDER BY s.changed_at, s.key_1
  LIMIT $4::int
`

type ExportTemplatesParams struct {
	Since   pgtype.Timestamptz
	AfterAt pgtype.Timestamptz
	AfterID pgtype.UUID
	Batch   int32
}

type ExportTemplatesRow struct {
	RecordID  string
	ChangedAt pgtype.Timestamptz
	Payload   []byte
}

func (q *Queries) ExportTemplates(ctx context.Context, arg ExportTemplatesParams) ([]ExportTemplatesRow, error) {
	rows, err := q.db.Query(ctx, exportTemplates,
		arg.Since,
		arg.AfterAt,
		arg.AfterID,
		arg.Batch,
	)
	if err != nil {
		return nil, err
	}
	defer rows.Close()
	items := []ExportTemplatesRow{}
	for rows.Next() {
		var i ExportTemplatesRow
		if err := rows.Scan(&i.RecordID, &i.ChangedAt, &i.Payload); err != nil {
			return nil, err
		}
		items = append(items, i)
	}
	if err := rows.Err(); err != nil {
		return nil, err
	}
	return items, nil
}

const exportTenants = `-- name: ExportTenants :many

SELECT s.record_id, s.changed_at, s.payload
FROM (
  SELECT (t.id)::text AS record_id,
         t.updated_at AS changed_at,
         (to_jsonb(t) - 'tenant_id')::jsonb AS payload,
         t.id AS key_1
  FROM tenant t
) s
  WHERE ($1::timestamptz IS NULL OR s.changed_at > $1::timestamptz)
    AND (s.changed_at, s.key_1) > ($2::timestamptz, $3::uuid)
  ORDER BY s.changed_at, s.key_1
  LIMIT $4::int
`

type ExportTenantsParams struct {
	Since   pgtype.Timestamptz
	AfterAt pgtype.Timestamptz
	AfterID pgtype.UUID
	Batch   int32
}

type ExportTenantsRow struct {
	RecordID  string
	ChangedAt pgtype.Timestamptz
	Payload   []byte
}

// The export the archive writer reads a tenant through (E-05, backup-restore.md §3, §5).
//
// One statement per entity, and they are as alike as the schema lets them be: an identity, when
// the row last changed, and the row itself as JSON with `tenant_id` taken out - a restore into
// another tenant must not carry the old one's identifier back in with it. Row level security
// supplies the tenant condition none of these statements writes (ADR-0010).
//
// Every statement pages on its own key rather than on OFFSET, so a run that is resumed after
// process death continues where it stopped instead of counting rows again - and so that a page
// never repeats or skips a row while the snapshot is open.
//
// **An entity is exported as a delta only if the schema lets it say when a row changed.** The ones
// that cannot - the join tables, and the configuration rows that carry a creation stamp and no
// change stamp - are exported whole in every archive, and a restore replaces their set rather than
// merging it. Deriving "unchanged" from a column that does not move is how an incremental chain
// silently loses an edit, and there is no column here to add that would not be a lie until every
// writer maintained it.
func (q *Queries) ExportTenants(ctx context.Context, arg ExportTenantsParams) ([]ExportTenantsRow, error) {
	rows, err := q.db.Query(ctx, exportTenants,
		arg.Since,
		arg.AfterAt,
		arg.AfterID,
		arg.Batch,
	)
	if err != nil {
		return nil, err
	}
	defer rows.Close()
	items := []ExportTenantsRow{}
	for rows.Next() {
		var i ExportTenantsRow
		if err := rows.Scan(&i.RecordID, &i.ChangedAt, &i.Payload); err != nil {
			return nil, err
		}
		items = append(items, i)
	}
	if err := rows.Err(); err != nil {
		return nil, err
	}
	return items, nil
}

const exportTombstones = `-- name: ExportTombstones :many
SELECT entity_id::text AS entity_id, deleted_at
FROM tombstone
WHERE entity = $1::text
  AND deleted_at > $2::timestamptz
  AND (deleted_at, entity_id) > ($3::timestamptz, $4::uuid)
ORDER BY deleted_at, entity_id
LIMIT $5::int
`

type ExportTombstonesParams struct {
	Entity  string
	Since   pgtype.Timestamptz
	AfterAt pgtype.Timestamptz
	AfterID pgtype.UUID
	Batch   int32
}

type ExportTombstonesRow struct {
	EntityID  string
	DeletedAt pgtype.Timestamptz
}

// One entity at a time, because each entity's deletions belong in its own data file - and because
// the primary key starts (tenant_id, entity), so asking for one entity is an index range rather
// than a scan. Paged on (deleted_at, entity_id) for the reason every statement above is: a page
// that repeated or skipped a row would put a tombstone in one archive and nowhere else.
func (q *Queries) ExportTombstones(ctx context.Context, arg ExportTombstonesParams) ([]ExportTombstonesRow, error) {
	rows, err := q.db.Query(ctx, exportTombstones,
		arg.Entity,
		arg.Since,
		arg.AfterAt,
		arg.AfterID,
		arg.Batch,
	)
	if err != nil {
		return nil, err
	}
	defer rows.Close()
	items := []ExportTombstonesRow{}
	for rows.Next() {
		var i ExportTombstonesRow
		if err := rows.Scan(&i.EntityID, &i.DeletedAt); err != nil {
			return nil, err
		}
		items = append(items, i)
	}
	if err := rows.Err(); err != nil {
		return nil, err
	}
	return items, nil
}

const exportWebhookSubscriptions = `-- name: ExportWebhookSubscriptions :many
SELECT s.record_id, s.changed_at, s.payload
FROM (
  SELECT (s.id)::text AS record_id,
         NULL::timestamptz AS changed_at,
         (to_jsonb(s) - 'tenant_id')::jsonb AS payload,
         s.id AS key_1
  FROM webhook_subscription s
) s
  WHERE s.key_1 > $1::uuid
  ORDER BY s.key_1
  LIMIT $2::int
`

type ExportWebhookSubscriptionsParams struct {
	AfterID pgtype.UUID
	Batch   int32
}

type ExportWebhookSubscriptionsRow struct {
	RecordID  string
	ChangedAt pgtype.Timestamptz
	Payload   []byte
}

func (q *Queries) ExportWebhookSubscriptions(ctx context.Context, arg ExportWebhookSubscriptionsParams) ([]ExportWebhookSubscriptionsRow, error) {
	rows, err := q.db.Query(ctx, exportWebhookSubscriptions, arg.AfterID, arg.Batch)
	if err != nil {
		return nil, err
	}
	defer rows.Close()
	items := []ExportWebhookSubscriptionsRow{}
	for rows.Next() {
		var i ExportWebhookSubscriptionsRow
		if err := rows.Scan(&i.RecordID, &i.ChangedAt, &i.Payload); err != nil {
			return nil, err
		}
		items = append(items, i)
	}
	if err := rows.Err(); err != nil {
		return nil, err
	}
	return items, nil
}

const exportWorkItems = `-- name: ExportWorkItems :many
SELECT s.record_id, s.changed_at, s.payload
FROM (
  SELECT (w.id)::text AS record_id,
         w.updated_at AS changed_at,
         (to_jsonb(w) - 'tenant_id')::jsonb AS payload,
         w.id AS key_1
  FROM work_item w
) s
  WHERE ($1::timestamptz IS NULL OR s.changed_at > $1::timestamptz)
    AND (s.changed_at, s.key_1) > ($2::timestamptz, $3::uuid)
  ORDER BY s.changed_at, s.key_1
  LIMIT $4::int
`

type ExportWorkItemsParams struct {
	Since   pgtype.Timestamptz
	AfterAt pgtype.Timestamptz
	AfterID pgtype.UUID
	Batch   int32
}

type ExportWorkItemsRow struct {
	RecordID  string
	ChangedAt pgtype.Timestamptz
	Payload   []byte
}

func (q *Queries) ExportWorkItems(ctx context.Context, arg ExportWorkItemsParams) ([]ExportWorkItemsRow, error) {
	rows, err := q.db.Query(ctx, exportWorkItems,
		arg.Since,
		arg.AfterAt,
		arg.AfterID,
		arg.Batch,
	)
	if err != nil {
		return nil, err
	}
	defer rows.Close()
	items := []ExportWorkItemsRow{}
	for rows.Next() {
		var i ExportWorkItemsRow
		if err := rows.Scan(&i.RecordID, &i.ChangedAt, &i.Payload); err != nil {
			return nil, err
		}
		items = append(items, i)
	}
	if err := rows.Err(); err != nil {
		return nil, err
	}
	return items, nil
}

const findMediaStorageKey = `-- name: FindMediaStorageKey :one
SELECT storage_key, byte_size
FROM media_object
WHERE checksum = $1::text
  AND status = 'READY'
  AND deleted_at IS NULL
ORDER BY created_at
LIMIT 1
`

type FindMediaStorageKeyRow struct {
	StorageKey string
	ByteSize   int64
}

// Where the bytes of one medium lie, by the checksum the archive addresses it with.
//
// READY only, and a checksum that is actually there: a PENDING upload is one whose bytes were
// never read back and judged (C-06, migration 0013), and an object with no recorded checksum has
// no content address at all. Neither is carried into an archive, and the row itself still is - so
// a restore keeps the metadata and knows the bytes are gone.
func (q *Queries) FindMediaStorageKey(ctx context.Context, checksum string) (FindMediaStorageKeyRow, error) {
	row := q.db.QueryRow(ctx, findMediaStorageKey, checksum)
	var i FindMediaStorageKeyRow
	err := row.Scan(&i.StorageKey, &i.ByteSize)
	return i, err
}

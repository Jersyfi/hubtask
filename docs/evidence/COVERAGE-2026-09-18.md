# COVERAGE — every use case of the catalogue, and where the web client reaches it

**2026-09-18, `main` after F6-01…F6-14 (milestone F6).** One row per use case
`catalogue.Descriptors()` returns — 231 today — with the web client's answer: the route and the
control that reaches it, or *omitted* with the reason. `0.9.5` asks for this table with a column
per client; F6-15 writes the web column, and convergence (F7) adds the two shells' columns to it
(`roadmap.md`). `tools/checkdocs` holds the first column to the catalogue in both directions
(milestone-F6.md decision 16): a use case added without a row, or a row naming no use case, turns
`make gate-docs` red.

| | Count |
|---|---|
| Reached from the web client | 213 |
| Omitted — no human surface (an agent's, a rule's, an app's) | 6 |
| Omitted — the operator's, reached through `hubctl` by `deployment.md`'s word | 10 |
| Omitted — nobody built it, and an issue names it | 2 |

The three reasons an omission may give are decision 16's; "nobody built it" is not a reason and
stands in the table only as the issue that will remove the row's omission. Routes are the
webapp's (`apps/webapp/src/lib/routes.ts`); a control is named as its screen names it, and the
store method beside it is where the use case is called.

| Use case | Web | Route and control, or the reason |
|---|---|---|
| `CreateContainer` | reached | `/` and `/hubs/:id` — *Create hub* / *Create collection* (`CreateContainerDialog`) |
| `CreateWorkItem` | reached | `/collections/:id`, `/items/:id` — *Add an entry* and the child form (`EntryList`, `items.create`); the tour's seventh step |
| `UpdateWorkItem` | reached | `/collections/:id`, `/items/:id` — the title and notes edited in place (`items.update`, `items.rewriteNotes`) |
| `RenameContainer` | reached | `/hubs/:id`, `/collections/:id` — *Rename* (`ContainerView`) |
| `UpdateContainerPolicies` | omitted — nobody built it | #773 — a collection's policies have no control; `hubctl container` reaches it |
| `ArchiveContainer` | reached | `/hubs/:id`, `/collections/:id` — *Archive* (`containers.setArchived`) |
| `UnarchiveContainer` | reached | `/hubs/:id`, `/collections/:id` — *Unarchive* (`containers.setArchived`) |
| `MoveContainer` | reached | `/collections/:id` — *Move to another hub…* (`MoveDialog`) |
| `ReorderContainer` | reached | `/hubs/:id`, `/collections/:id` — *Move up* / *Move down* (`containers.reorder`) |
| `TrashContainer` | reached | `/hubs/:id`, `/collections/:id` — *Move to the trash* (`ContainerView`) |
| `RestoreContainer` | reached | `/trash` — *Restore* (`TrashView`) |
| `CreateBucket` | reached | `/collections/:id` (board) — *Add a column* (`Board`, `buckets.create`) |
| `ListBuckets` | reached | `/collections/:id` (board) — the columns (`buckets.open`) |
| `UpdateBucket` | reached | `/collections/:id` (board) — the column's rename and done-mark (`buckets.update`) |
| `ReorderBucket` | reached | `/collections/:id` (board) — the column's move (`buckets.reorder`) |
| `DeleteBucket` | reached | `/collections/:id` (board) — *Delete the column* (`buckets.remove`) |
| `CreateLabel` | reached | `/collections/:id` — *Labels* dialog, *Define* (`LabelsDialog`) |
| `ListLabels` | reached | `/collections/:id`, `/items/:id` — the label picker and the dialog (`labels.open`) |
| `UpdateLabel` | reached | `/collections/:id` — *Labels* dialog, edit (`labels.update`) |
| `DeleteLabel` | reached | `/collections/:id` — *Labels* dialog, delete (`labels.remove`) |
| `AddLabel` | reached | `/collections/:id`, `/items/:id` — the label picker on a row (`LabelPicker`, `labels.setOnItem`) |
| `RemoveLabel` | reached | `/collections/:id`, `/items/:id` — the label picker on a row (`LabelPicker`) |
| `AssignWorkItem` | reached | `/collections/:id`, `/items/:id` — the assignee control (`AssigneeControl`, `items.assign`) |
| `UnassignWorkItem` | reached | `/collections/:id`, `/items/:id` — the assignee control, *nobody* (`items.unassign`) |
| `AutoAssignWorkItem` | reached | `/items/:id` — *Assign by the collection's rule* (`items.autoAssign`) |
| `AddMember` | reached | `/items/:id` — the members control (`items.addMember`) |
| `RemoveMember` | reached | `/items/:id` — the members control (`items.removeMember`) |
| `AddComment` | reached | `/items/:id` — the thread's composer (`CommentThread`, `comments.add`) |
| `ListComments` | reached | `/items/:id` — the thread (`comments.open`) |
| `EditComment` | reached | `/items/:id` — the thread's edit on an own comment (`comments.edit`) |
| `DeleteComment` | reached | `/items/:id` — the thread's delete on an own comment (`comments.remove`) |
| `GetContainer` | reached | `/hubs/:id`, `/collections/:id` — the screen reads it (`containers.openSingle`) |
| `ListContainers` | reached | every route — the sidebar tree (`WorkspaceNav`) and a hub's collections |
| `GetWorkItem` | reached | `/items/:id` — the entry screen (`item.svelte.ts`) |
| `ListWorkItems` | reached | `/items/:id` — the children under an entry (`items.childrenOf`); the tour's first entry |
| `QueryItems` | reached | `/collections/:id` — the list and the board (`items.inCollection`, `/items:query`) |
| `SearchItems` | reached | `/search` — the search field (`SearchView`) |
| `ReindexSearch` | omitted — the operator's | `hubctl search reindex` (`deployment.md`); a maintenance pass with no screen |
| `ListActivity` | reached | `/items/:id` — the activity feed (`ActivityFeed`, `activity.ts`) |
| `CompleteWorkItem` | reached | `/collections/:id`, `/items/:id`, `/search` — the row's check (`items.setCompleted`) |
| `ReopenWorkItem` | reached | `/collections/:id`, `/items/:id`, `/search` — the row's check again (`items.setCompleted`) |
| `MoveWorkItem` | reached | `/collections/:id` — the row menu's *Move…* (`MoveDialog`, `items.move`) |
| `DuplicateWorkItem` | reached | `/collections/:id` — the row menu's *Duplicate* (`DuplicateDialog`) |
| `BulkUpdateWorkItems` | reached | `/collections/:id` — the selection bar (`BulkBar`, `items.bulk`) |
| `ReorderWorkItem` | reached | `/collections/:id` — the row menu's rank verbs and the drag (`items.reorder`) |
| `ArchiveWorkItem` | reached | `/items/:id` — *Archive* (`items.setArchived`) |
| `UnarchiveWorkItem` | reached | `/items/:id` — *Unarchive* (`items.setArchived`) |
| `TrashWorkItem` | reached | `/collections/:id`, `/items/:id` — *Move to the trash* (`items.trash`) |
| `RestoreWorkItem` | reached | `/trash` — *Restore* (`items.restore`) |
| `ListTrash` | reached | `/trash` — the listing (`TrashView`) |
| `PurgeWorkItem` | reached | `/trash` — *Delete for good* on one entry (`items.purge`) |
| `EmptyTrash` | reached | `/trash` — *Empty the trash* (`TrashView`) |
| `RequestMediaUpload` | reached | `/items/:id` — a cover or an attachment chosen (`media.upload`); `/hubs/:id` — *Import…* |
| `ConfirmMediaUpload` | reached | `/items/:id`, `/hubs/:id` — the third step of `media.upload` |
| `SetCover` | reached | `/items/:id` — the cover panel (`CoverPanel`, `media.setCover`) |
| `ClearCover` | reached | `/items/:id` — the cover panel, *Remove* (`media.clearCover`) |
| `SetDueDate` | reached | `/collections/:id`, `/items/:id` — the due control (`DueDateControl`, `items.setDue`) |
| `ClearDueDate` | reached | `/collections/:id`, `/items/:id` — the due control, *Clear* (`items.clearDue`) |
| `CreateReminder` | reached | `/items/:id` — the reminders editor (`ReminderEditor`, `reminders.add`) |
| `ListReminders` | reached | `/items/:id` — the reminders editor (`reminders.open`) |
| `UpdateReminder` | reached | `/items/:id` — the reminders editor (`reminders.edit`) |
| `DeleteReminder` | reached | `/items/:id` — the reminders editor, *Remove* (`reminders.remove`) |
| `SetRecurrence` | reached | `/items/:id` — the recurrence editor (`RecurrenceEditor`, `recurrence.set` in `reminders.svelte.ts`) |
| `RemoveRecurrence` | reached | `/items/:id` — the recurrence editor, *Stop repeating* (`recurrence.remove`) |
| `SkipOccurrence` | reached | `/items/:id` — the recurrence editor, *Skip the next* (`recurrence.skip`) |
| `CreateTemplate` | reached | `/collections/:id` — *Templates*, *Define a template* (`TemplatesDialog`); accepting a generated draft |
| `ListTemplates` | reached | `/collections/:id` — *Templates* (`templates.open`) |
| `GetTemplate` | omitted — no human surface | an agent's: the web client reads the listing, which carries every template whole (`ListTemplates`) |
| `UpdateTemplate` | reached | `/collections/:id` — *Templates*, *Edit* (`templates.update`) |
| `DeleteTemplate` | reached | `/collections/:id` — *Templates*, *Delete* (`templates.remove`) |
| `InstantiateTemplate` | reached | `/collections/:id` — *Templates*, *Use* (`templates.instantiate`) |
| `GetRecurrence` | reached | `/items/:id` — the recurrence editor reads it (`recurrencePath`) |
| `AttachMedia` | reached | `/items/:id` — the attachments panel (`AttachmentPanel`, `media.attach`) |
| `DefineCustomField` | reached | `/collections/:id` — *Fields*, *Define* (`CustomFieldsDialog`, `customFields.define`) |
| `ListCustomFields` | reached | `/collections/:id`, `/items/:id` — the definitions in force (`customFields.of`) |
| `UpdateCustomField` | reached | `/collections/:id` — *Fields*, *Edit* (`customFields.update`) |
| `DeleteCustomField` | reached | `/collections/:id` — *Fields*, *Delete* (`customFields.remove`) |
| `ExportView` | reached | `/collections/:id` — *Views*, *Export…* (`ExportDialog`, `views.export`) |
| `CreateCalendarFeed` | reached | `/collections/:id` — *Views*, *Subscribe a calendar* (`FeedsDialog`, `feeds.create`) |
| `ListCalendarFeeds` | reached | `/collections/:id` — *Views*, the subscriptions (`feeds.all`) |
| `RevokeCalendarFeed` | reached | `/collections/:id` — *Views*, the subscriptions, *Revoke* (`feeds.revoke`) |
| `CreateSavedView` | reached | `/collections/:id` — *Views*, *Save* (`ViewsPanel`, `views.create`) |
| `ListSavedViews` | reached | `/collections/:id` — *Views* (`views.open`) |
| `GetSavedView` | omitted — no human surface | an agent's: the web client reads the listing, which carries every view whole (`ListSavedViews`) |
| `UpdateSavedView` | reached | `/collections/:id` — *Views*, *Rename* / *Replace with the current query* (`views.update`) |
| `DeleteSavedView` | reached | `/collections/:id` — *Views*, *Delete* (`views.remove`) |
| `ShareSavedView` | reached | `/collections/:id` — *Views*, *Share* (`views.share`) |
| `SetCustomField` | reached | `/items/:id` — the field's control (`CustomFieldRenderer`, `customFields.setValue`) |
| `DetachMedia` | reached | `/items/:id` — the attachments panel, *Detach* (`media.detach`) |
| `ListAttachments` | reached | `/items/:id` — the attachments panel (`attachmentsPath`) |
| `GetMedia` | reached | `/items/:id` — a cover drawn or an attachment opened (`media.recordOf`, `media.downloadUrl`) |
| `DeleteMedia` | omitted — no human surface | an agent's: the client detaches, and the reconciliation job removes an object nothing points at (`media.md`); a person never deletes bytes by hand |
| `CreateBackupTarget` | reached | `/administration/backup` — *Add a target* (`BackupView`, `backup.createTarget`) |
| `DeleteBackupTarget` | reached | `/administration/backup` — the target's *Remove* (`backup.deleteTarget`) |
| `ListBackupTargets` | reached | `/administration/backup` — the targets (`backup.open`) |
| `TestBackupTarget` | reached | `/administration/backup` — the target's *Test* (`backup.test`) |
| `StartBackup` | reached | `/administration/backup` — *Back up now* (`backup.start`) |
| `GetBackupRun` | reached | `/administration/backup` — the run's row and its job (`backup.readRun`, `jobs.watch`) |
| `VerifyBackup` | reached | `/administration/backup` — the run's *Verify* (`backup.verify`) |
| `ListBackupsAtTarget` | reached | `/administration/restore` — *Read from* a target lists its archives (`backup.archivesAt`) |
| `StartRestore` | reached | `/administration/restore` — *Rehearse* and *Run for real* (`RestoreView`, `restores.start`) |
| `GetRestoreRun` | reached | `/administration/restore` — the report (`restores.read`) |
| `CreateRetentionPolicy` | reached | `/administration/retention` — *Add a policy* (`RetentionView`, `policies.create`) |
| `ListRetentionPolicies` | reached | `/administration/retention` — the policies (`policies.open`) |
| `UpdateRetentionPolicy` | reached | `/administration/retention` — the policy's edit (`policies.update`) |
| `DeleteRetentionPolicy` | reached | `/administration/retention` — the policy's *Withdraw* (`policies.withdraw`) |
| `PreviewRetentionPolicy` | reached | `/administration/retention` — *Preview what it would take* (`policies.preview`) |
| `RetainItem` | reached | `/items/:id` — *Keep this entry* against a pending removal (`policies.retain`) |
| `PlaceLegalHold` | reached | `/administration/retention` — *Place a hold* (`policies.place`) |
| `ReleaseLegalHold` | reached | `/administration/retention` — the hold's *Release* (`policies.release`) |
| `ListLegalHolds` | reached | `/administration/retention` — the holds (`policies.holds`) |
| `ListAuditEntries` | reached | `/administration/audit` — the trail with its filters (`AuditView`, `audit.open`) |
| `VerifyAuditChain` | reached | `/administration/audit` — *Verify the chain* (`audit.verify`) |
| `ConfigureAuditAnchoring` | omitted — nobody built it | #774 — no control in the web client and no `hubctl` verb; the audit screen says *never anchored* |
| `ExportAuditTrail` | reached | `/administration/audit` — *Export* (`audit.export`, the job followed) |
| `ImportEntries` | reached | `/hubs/:id` — *Import…* (`ImportDialog`, `imports.start`) |
| `GetImport` | reached | `/hubs/:id` — *Import…*, the report when the job ends (`imports.read`) |
| `CreateDataSubjectRequest` | reached | `/administration/privacy` — *Record a request* (`PrivacyView`, `privacy.record`) |
| `ListDataSubjectRequests` | reached | `/administration/privacy` — the requests (`privacy.open`) |
| `UpdateDataSubjectRequest` | reached | `/administration/privacy` — the request's state change (`privacy.change`) |
| `RestrictProcessing` | reached | `/administration/privacy` — *Restrict processing* (`privacy.restrict`) |
| `WithdrawConsent` | reached | `/profile` — *Withdraw* on the AI consent (`privacy.withdraw`) |
| `CreateBackupSchedule` | reached | `/administration/backup` — *Schedule* (`backup.createSchedule`) |
| `ListBackupSchedules` | reached | `/administration/backup` — the schedules (`backup.open`) |
| `UpdateBackupSchedule` | reached | `/administration/backup` — the schedule's edit (`backup.updateSchedule`) |
| `DeleteBackupSchedule` | reached | `/administration/backup` — the schedule's *Remove* (`backup.deleteSchedule`) |
| `GetJob` | reached | every screen that starts a job — `jobs.watch` polls it (backup, restore, audit export, import) |
| `CancelJob` | reached | `/administration/backup`, `/administration/restore` — *Cancel* while a job runs (`jobs.cancel`) |
| `InviteAccount` | reached | `/administration/people` — *Invite* (`PeopleView`, `people.invite`) |
| `GetOwnAccount` | reached | every route — the frame reads `/accounts/me` (`actor`) |
| `GetAccount` | reached | every screen naming a person — the name behind an identifier (`accounts.resolve`) |
| `UpdateAccountPreferences` | reached | `/profile` — *Save* on language, time zone and week start; the celebrations switch; the tour (`preferences.setAccount`) |
| `ListNotificationPreferences` | reached | `/profile` — the notification switches (`preferences.open`) |
| `SetNotificationPreference` | reached | `/profile` — a notification switch (`preferences.setNotification`) |
| `ListMemberships` | reached | `/hubs/:id`, `/collections/:id`, `/items/:id` — *People* (`MembersDialog`, `people.along`) |
| `GrantMembership` | reached | `/hubs/:id`, `/collections/:id`, `/items/:id` — *People*, *Grant* (`people.grant`) |
| `RevokeMembership` | reached | `/hubs/:id`, `/collections/:id`, `/items/:id` — *People*, *Revoke* (`people.revoke`) |
| `ListGroups` | reached | `/administration/groups` — the groups (`GroupsView`, `groups.open`) |
| `GetGroup` | reached | `/administration/groups` and *People* — a group's members resolved (`groups.read`, `people.membersOf`) |
| `CreateGroup` | reached | `/administration/groups` — *Create a group* (`groups.create`) |
| `UpdateGroup` | reached | `/administration/groups` — the group's edit and its member list (`groups.update`) |
| `DeleteGroup` | reached | `/administration/groups` — the group's *Delete* (`groups.remove`) |
| `SignIn` | reached | `/sign-in` — the form (`SignInView`, `session.signIn`) |
| `RefreshSession` | reached | every route — the engine's one exchange on a refused request (`engine.ts`, `onRefresh`) |
| `ListSessions` | reached | `/profile` — the sessions (`sessions.open`) |
| `RevokeSession` | reached | `/profile` — a session's *Sign out there* (`sessions.end`) |
| `RevokeAllSessions` | reached | `/profile` — *Sign out everywhere* (`sessions.endAll`) |
| `ListSyncDevices` | reached | `/profile` — the devices (`devices.open`) |
| `ForgetSyncDevice` | reached | `/profile` — a device's *Forget* (`devices.forget`) |
| `RedeemInvitation` | reached | `/redeem` — the redemption form (`RedeemView`) |
| `CompleteSignIn` | reached | `/sign-in` — the second factor's code (`SignInView`, `session.verify`) |
| `EnrollTotp` | reached | `/profile` — *Set up a second factor* (`TotpEnrollment`, `mfa.start`) |
| `ConfirmTotp` | reached | `/profile` — the enrolment's confirmation code (`mfa.confirm`) |
| `DisableTotp` | reached | `/profile` — *Switch the second factor off* (`mfa.disable`) |
| `StepUp` | reached | every route — the frame's prompt when a privileged action asks for it (`StepUpPrompt`, `stepUp.around`) |
| `RegisterOauthClient` | reached | `/administration/apps` — *Register an app* (`AppsView`, `apps.register`) |
| `ListOauthClients` | reached | `/administration/apps` — the apps (`apps.open`) |
| `ReadOauthClient` | reached | `/oauth/consent` — the consent screen names the app (`ConsentView`, `consent.appNamed`) |
| `DeleteOauthClient` | reached | `/administration/apps` — the app's *Remove* (`apps.remove`) |
| `AuthorizeOauthClient` | reached | `/oauth/consent` — *Allow* (`ConsentView`, `consent.approve`) |
| `ExchangeOauthCode` | omitted — no human surface | an app's: the token endpoint is called by the client that was authorised, never by a person |
| `ListOauthGrants` | reached | `/profile` — the apps allowed (`consent.openGrants`) |
| `RevokeOauthGrant` | reached | `/profile` — an allowed app's *Revoke* (`consent.withdraw`) |
| `ReadWorkspace` | reached | `/administration/workspace` — the settings (`WorkspaceSettingsView`, `workspace.open`) |
| `UpdateWorkspace` | reached | `/administration/workspace` — *Save* (`workspace.change`) |
| `ConfigureIdentityProvider` | reached | `/administration/identity-provider` — *Save* (`IdentityProviderView`, `identityProvider.configure`) |
| `ReadIdentityProvider` | reached | `/administration/identity-provider` — the form reads it (`identityProvider.open`) |
| `RemoveIdentityProvider` | reached | `/administration/identity-provider` — *Remove* (`identityProvider.remove`) |
| `ConfigureAiProvider` | reached | `/administration/ai` — *Save* (`AiSettingsView`, `aiProvider.configure`) |
| `ReadAiProvider` | reached | `/administration/ai` — the form reads it (`aiProvider.open`) |
| `RemoveAiProvider` | reached | `/administration/ai` — *Remove* (`aiProvider.remove`) |
| `ListSuggestions` | reached | `/items/:id`, `/collections/:id`, `/jumble` — the strips (`suggestionsPath`) |
| `AcceptSuggestion` | reached | `/items/:id`, `/collections/:id`, `/jumble` — the strip's accept verb (`suggestions.accept`) |
| `DismissSuggestion` | reached | `/items/:id`, `/collections/:id`, `/jumble` — *Dismiss* (`suggestions.dismiss`) |
| `SuggestDecomposition` | reached | `/items/:id` — *Break it down* (`SuggestionStrip`, `suggestions.ask('decompose')`) |
| `AiSuggestFields` | reached | `/items/:id` — *Suggest fields* (`suggestions.ask('suggest-fields')`) |
| `AiSummarize` | reached | `/items/:id` — *Summarise* (`suggestions.ask('summarize')`) |
| `AiClassify` | reached | `/items/:id` — *Classify* (`suggestions.ask('classify')`) |
| `SuggestDuplicates` | reached | `/items/:id` — *Find look-alikes* (`suggestions.ask('duplicates')`) |
| `AiSummarizeThread` | reached | `/items/:id` — *Summarise the discussion* (`suggestions.ask('summarize-thread')`) |
| `AiSummarizeContainer` | reached | `/collections/:id` — *Summarise* (`CollectionSummary`, `suggestions.askContainer`) |
| `AiGenerateTemplate` | reached | `/collections/:id` — *Templates*, *Generate from a description…* (`suggestions.generateTemplate`) |
| `AiTranslate` | reached | `/items/:id` — *Read in another language* (`suggestions.translate`) |
| `StartOidcSignIn` | reached | `/sign-in` — *Sign in with the identity provider* (`oidc.begin`) |
| `CompleteOidcSignIn` | reached | `/auth/callback` — the callback screen (`OidcCallbackView`, `oidc.complete`) |
| `CreateAccessToken` | reached | `/profile/tokens` — *Mint a token* (`TokensView`, `tokens.mint`) |
| `ListAccessTokens` | reached | `/profile/tokens` — the tokens (`tokens.open`) |
| `RevokeAccessToken` | reached | `/profile/tokens` — a token's *Withdraw* (`tokens.revoke`) |
| `CreateServiceAccount` | reached | `/administration/service-accounts` — *Create* (`ServiceAccountsView`, `serviceAccounts.create`) |
| `ListServiceAccounts` | reached | `/administration/service-accounts` — the listing (`serviceAccounts.open`) |
| `CreateWebhookSubscription` | reached | `/administration/webhooks` — *Subscribe* (`WebhooksView`, `webhooks.subscribe`) |
| `GetWebhookSubscription` | reached | `/administration/webhooks` — a subscription opened (`webhooks.open`) |
| `ListWebhookSubscriptions` | reached | `/administration/webhooks` — the subscriptions (`webhooks.open`) |
| `UpdateWebhookSubscription` | reached | `/administration/webhooks` — the subscription's edit (`webhooks.update`) |
| `DeleteWebhookSubscription` | reached | `/administration/webhooks` — *Remove* (`webhooks.unsubscribe`) |
| `ListWebhookDeliveries` | reached | `/administration/webhooks` — the deliveries (`deliveriesPath`) |
| `ReplayWebhookDelivery` | reached | `/administration/webhooks` — a delivery's *Replay* (`webhooks.replay`) |
| `SendWebhook` | reached | `/administration/webhooks` — *Send a test* (`webhooks.send`) |
| `RotateWebhookSecret` | reached | `/administration/webhooks` — *Rotate the secret* (`webhooks.rotate`) |
| `PollTriggerEvents` | omitted — no human surface | an integration's: the pull half of a webhook, called by Zapier and n8n (`packages/zapier-app`, `packages/n8n-nodes-hubtask`) |
| `CreateRule` | reached | `/administration/rules` — *Add a rule* (`RulesView`, `rules.write`) |
| `GetRule` | reached | `/administration/rules` — a rule opened (`rules.open`) |
| `ListRules` | reached | `/administration/rules` — the rules (`rules.open`) |
| `UpdateRule` | reached | `/administration/rules` — the rule's edit (`rules.change`) |
| `EnableRule` | reached | `/administration/rules` — *Enable* (`rules.enable`) |
| `DisableRule` | reached | `/administration/rules` — *Disable* (`rules.disable`) |
| `DeleteRule` | reached | `/administration/rules` — *Delete* (`rules.remove`) |
| `TriggerRuleManually` | reached | `/administration/runs` — *Trigger* (`runs.trigger`) |
| `RotateInboundTrigger` | reached | `/administration/rules` — *Make the address* / *Make a new address* beside an inbound rule, answered once (`rules.rotateInbound`; #775) |
| `ListRuleRuns` | reached | `/administration/runs` — the runs (`runs.open`) |
| `GetRuleRun` | reached | `/administration/runs` — a run opened, every step (`runs.detail`) |
| `HttpRequest` | omitted — no human surface | a rule's: the HTTP action a run performs (`automation.md` §1.3); no person calls an address by hand |
| `TestRule` | reached | `/administration/rules` — *Test with an event* (`runs.dryRun`) |
| `ReplayRuleRun` | reached | `/administration/runs` — a run's *Replay* (`runs.replay`) |
| `SubmitJumbleEntry` | reached | `/jumble` — *Jot something down* (`JumbleView`, `jumble.capture`) |
| `ListJumbleEntries` | reached | `/jumble` — the entries by state (`jumble.open`) |
| `ConvertJumbleEntry` | reached | `/jumble` — *Make it an entry* (`JumbleProposal`, `jumble.convert`) |
| `DismissJumbleEntry` | reached | `/jumble` — *Dismiss* (`jumble.dismiss`) |
| `SuggestFromJumbleEntry` | reached | `/jumble` — *Ask what this could be* (`suggestions.askJumble`) |
| `RotateJumbleIntake` | reached | `/jumble` — *Rotate the intake address* (`jumble.rotateIntake`) |
| `ProvisionTenant` | omitted — the operator's | `hubctl tenant create` on the control plane (`deployment.md` §3); crosses workspaces |
| `ListTenants` | omitted — the operator's | `hubctl tenant ls` on the control plane; the one legitimate tenant enumerator |
| `SuspendTenant` | omitted — the operator's | `hubctl tenant pause`; an operator's act on a workspace, never from inside one |
| `ResumeTenant` | omitted — the operator's | `hubctl tenant resume` |
| `RequestTenantDeletion` | omitted — the operator's | `hubctl tenant rm`; the purge deadline the listing shows |
| `ExportTenant` | omitted — the operator's | `hubctl tenant export`; a workspace's own export is the backup screen's |
| `UpdateTenantQuotas` | omitted — the operator's | `hubctl quota set`; the quotas screen shows the ceilings and says the control is the operator's (`quotas.svelte.ts`) |
| `ReadQuotas` | reached | `/administration/quotas` — the ceilings and the counts (`QuotasView`, `quotas.open`) |
| `GetHealthReport` | reached | every route — the frame's notice where the actor may read it (`HealthNotice`, `health.start`); `/installation` |
| `ReadEncryptionStatus` | omitted — the operator's | `hubctl admin encryption show`; installation-wide, `admin:tenants` |
| `ResealSecrets` | omitted — the operator's | `hubctl admin encryption reseal` |
